# Econovaria Complete Development Roadmap

**Document ID:** `ECON-BETA-ROADMAP-V1`  
**Roadmap authority:** Chat 1  
**Audited main:** `80f5eb8e24a364bc878de11acfdf196add878f10`
**Audit date:** 2026-08-31
**Current decision:** `BLOCKED`  
**Production deployment authorized:** No

The detailed stable capability definitions and acceptance criteria in the prior roadmap revision remain authoritative unless changed by the live controller records below. Current status, ownership, merge order, migration collision decisions, and exact next actions are governed by:

- `docs/roadmaps/econovaria-beta-live-reconciliation-v3.md`;
- `docs/operations/econovaria-beta-coordination-matrix-v1.md`;
- `docs/operations/econovaria-beta-controller-reconciliation-2026-07-21.md`.

Use only `VERIFIED_COMPLETE`, `IMPLEMENTED_NOT_MERGED`, `IN_PROGRESS`, `PLANNED`, `BLOCKED`, and `RE_AUDIT_REQUIRED`.

PR #296 is closed without merge. PR #298 is the preceding merged controller authority. PRs #248, #249, and #261 are explicitly reactivated. PRs #299 and #300 are active authorities.

The mandatory queue is #163, #294, #299, #300, #249, #248, #261, shared convergence, #295, continuous pilot, and final go/no-go. Production promotion requires a separate explicit product-owner instruction.

## Scope Intake

- **`BUSINESS-V2-14D` — Financial Market integration (2026-09-18)**
  - Status: `IMPLEMENTED_NOT_MERGED`, exact certified source `dee709fcbe5bfdf23c66347447eeb70c1408dcd9`, draft PR #687 / `feat/business-financial-market-integration-v2`, from green C handoff `1b1af1d492cc0f9dfe55dfa0f6a326106be3f8ea`.
  - Business publishes versioned evidence and remains the sole common-share authority. Market consumes completed IPOs, owns listing/prices and transfers finite custody through Business ports; existing Banking commands settle cash atomically. Player Market, Portfolio and Dashboard show authoritative shares, exact closed financials and separate currency values. Five forward migrations, RPCs, implementation files and rollout are in `business-financial-market-scope-v1.md`.
  - All 43 workflows, 86 applicable jobs and Vercel passed; 4 manual/historical jobs skipped. Market run `35332193613` passed two fresh replays/advisors, economic/race/rollback/replay/purge acceptance and all 14 desktop/mobile cases. Connected run `35332193485` passed Business/World, 30/40-player load and final enforcement. Complete identities and artifact digest are in the checkpoint.
  - Full Phase 14A–D is repository-certified on draft PRs #684–#687. No remaining Phase 14 repository implementation. No merge, deployment, live SQL, scheduler or secret action; live parity and later integration/release remain separate.

- **`BUSINESS-V2-14C` — Fixed-price primary IPO (2026-09-18)**
  - Status: `IMPLEMENTED_NOT_MERGED`, exact source `cf3fabf2444a2c85d71155fccfa1e5b371bc2c37`, draft PR #686 / `feat/business-ipo-issuance-v2`, from green Phase 14B handoff `bab8393cd8f0d30a84be5ad36ad90f41f9cae75f`.
  - Canonical reconciled-statement eligibility, immutable approved terms, atomic Checking-to-Business common issuance, explicit operating mandates and authenticated Player review are implemented. Four forward migrations end at `20260918083200_business_primary_ipo_purge_convergence_v1.sql`. Scope, files, RPCs, corrections and rollout are recorded in `business-primary-ipo-scope-v1.md`.
  - All 43 workflows, 84 applicable jobs and Vercel passed; four manual/historical jobs skipped as intended. IPO run `35326213193` passed two fresh replays/advisors, canonical economics, allocation race, populated purge and all 12 desktop/mobile cases. Connected run `35326213393` passed Business/World, both classroom loads and final enforcement. Full identities and artifact digest are in the checkpoint.
  - Successor `BUSINESS-V2-14D` is now repository-certified on draft #687. No merge, deployment, live SQL, scheduler or secret change. Live parity remains separate.

- **`BUSINESS-V2-14B` — Common-equity invariants (2026-09-18)**
  - Status: `IMPLEMENTED_NOT_MERGED`, exact certified implementation `197caa4034214e21b8033a3f4363dc07a13b5dcf`. Owner draft PR #685 / `feat/business-common-equity-v2`, exact green predecessor handoff `bcc53a5f6f4c3dcca1938e385e63239d59cdad7a`; no replacement reporting owner.
  - CLI-created forward enforcement migration, permanent source/two-replay database/race acceptance and retained stack CI filters are prepared. Canonical positions, immutable receipts and corporate share structure remain sole authorities; common shares reconcile one share/one vote, receipts and authorized capacity. Raw service cap-table writes are revoked; existing definer commands and request-bound game purge remain intact.
  - Files, behavior, compatibility audit, verification and rollout: `business-common-equity-scope-v1.md`. No new table/FK, Market/money/Inventory writer, HTTP route, merge, deployment or live SQL. All 28 exact-source workflows, 53 applicable jobs and Vercel passed; three manual staging/parity jobs skipped. Common-equity run `35320024755` passed two fresh replays/race/advisors; connected `35320024704` passed all journeys and both classroom loads. Full evidence and earlier fixture-failure limits are in the checkpoint.
  - Successors `BUSINESS-V2-14C` and `14D` are now repository-certified on drafts #686/#687. IPO investment ownership remains separate from operating/treasury authority.


- **`BUSINESS-V2-14A`–`14D` — Full Phase 14 continuation (2026-09-18)**
  - Status: `IMPLEMENTED_NOT_MERGED`; all four Phase 14 stages are repository-certified. The owner instruction “continue until phase 14 is done” has been fulfilled for the authorized implementation and verification scope. Existing reporting owner PR #684 remains authoritative; successors retain the exact-green-predecessor sequence below. No deployment, live SQL, secret or scheduler change is authorized.
  - Phase 14A: `IMPLEMENTED_NOT_MERGED` on draft PR #684 at exact certified source `aed8c3f6462d927cec3ebc55602854d48a460a9b`. Five CLI-generated forward migrations (`20260918014630` through `20260918035524`) implement closed-period evidence, immutable inventory/loan observations, reconciled income/balance/cash-flow statements, authenticated Player/Admin reads and canonical purge convergence. Player Finance and Admin's 25 read-only sections preserve exact decimal text, separate currencies, historical coverage, privacy and stale/error states. Implementation files, RPCs, rollout and acceptance limits are in `business-financial-reporting-scope-v1.md`.
  - Verification at that source: all 50 applicable workflows, 90 checks and Vercel passed. One staging-only workflow and eight manual/historical jobs skipped as expected. Reporting run `35314969708` passed source/database/browser, two fresh replays and advisors; all six desktop/mobile cases passed. Connected run `35314969894` passed Business/World, both classroom load profiles and final enforcement. Full workflow/job/artifact identities and skip reasons are in the checkpoint. No required economic or connected gate was skipped.
  - All four checkpoints are now repository-certified; final 14D implementation `dee709fcbe5bfdf23c66347447eeb70c1408dcd9` on draft #687 passed its full matrix. Stack remains `IMPLEMENTED_NOT_MERGED`; no further implementation checkpoint remains in Phase 14.
  - The prior reporting handoff's retained Store browser failure passed unchanged on rerun (run `35298549687`, job `105468946753`); no retained assertion was weakened. Live parity remains a separate release/runtime blocker.

- **`BUSINESS-V2-14A1` — Closed-period financial reporting foundation (historical checkpoint)**
  - Status: `IMPLEMENTED_NOT_MERGED`. Draft PR #684, owner `feat/business-financial-reporting-v2`, from merged Phase 13 main `4a9674ed9ca0565c828093fe100aa658ef4faaa6`. Exact verified implementation `c3647db988f2be8edd52b1656e640b388b2fc00f`; at that historical checkpoint, Phase 14A and the full phase remained `IN_PROGRESS`; the later certificates above supersede that state.
  - Scope: read-only, owner-scoped reporting over immutable closed-period receipts; exact amounts by currency and bounded history. Full statements await labor capitalization and period-end reconciliation.
  - Implementation: CLI-generated forward migration `20260918014630_business_financial_reporting_v1.sql` adds service-only `read_owned_business_financial_reports_v1(uuid,uuid)` and two private read/projection functions. Permanent workflow `.github/workflows/business-financial-reporting.yml` and `scripts/business-financial-reporting-*.mjs` retain source, two fresh database replays, privacy/ownership/precision and 51-period acceptance. No HTTP route or UI is added.
  - Scope/acceptance/rollout: `docs/roadmaps/business-financial-reporting-scope-v1.md`. Exact tested implementation and workflow evidence belong to this bounded draft PR. No full statements, UI, equity/IPO, deployment or live SQL completion is claimed.
  - Verification: 25 applicable workflows, 50 checks and Vercel passed; one staging-only workflow and six manual release/staging/preview checks skipped. Reporting run `35297504381` passed source `105452976974`, database `105452976705`, two fresh replays and advisors. Connected run `35297504137` / job `105453070102` passed Business/World, 30/40-player load and final enforcement. Full identities and expected skips are in `business-v2-current-checkpoint-v1.json`. Initial fixture overlap was repaired with 51 contiguous canonical closes; no economic guard or assertion was weakened.
  - Runtime/release: no staging/production deployment or live SQL. `BETA-LIVE-MIGRATION-PARITY-001` remains separate. Documentation handoff commits preserve the exact tested implementation above.
  - Historical next step completed by the subsequent Phase 14A–D certificates above.

- **`BETA-BUSINESS-MERGE-RELEASE-GUARD-001` — Separate merge from deployment**
  - Status: `VERIFIED_COMPLETE`. PR #683 merged as `0aeac221679da5fb4beac8f027f6697905537d5a`; Phase 13 also guards Admin production cutover. All three deployment paths skipped on Business main merges and `release/production` remained unchanged.
  - Owner instruction on 2026-09-18 authorizes merging certified Phase 12 PR #681 and Phase 13 PR #682, then starting `BUSINESS-V2-14A`. This supersedes the older Business no-merge/no-Phase-14 scope below. Deployment, live SQL, secret and scheduler changes remain outside the request.
  - Merge preflight found that main-push Edge convergence deploys staging and production, and Production Git Release can advance the live release branch. Bound both to explicit manual release dispatch; retain exact-main, staging, parity, inventory and production-environment gates. Pushes retain static verification only.
  - Existing PR #620 owns dependency action-version updates on these workflows, not this release-authorization correction. Its action pins were preserved; any later dependency merge must retain the dispatch authorization guards. Phase 14 now has draft owner PR #684.
  - Phase 12 source `76539c5cfcff612a322963e303e727e6edc7f7ed` / handoff `d6ddb52f38da1ad931ba56600b49786f11f11ac6` and Phase 13 source `949da9f91990eeeee2cd815541446a5b64130664` / handoff `c68bd1df07bc654ba9f59856b91f74a581da872e` retain their certifications. Both are merged; exact merge candidates, checks and identities are retained in `docs/operations/evidence/business-phase12-phase13-merge-2026-09-18.json`.
  - Files: the two release workflows, their Vercel and retained Phase 11 source contracts, production change control and this ledger; Phase 13 also guards Admin production cutover. No application, schema, RPC or live environment change came from these guards. All 26 applicable Phase 13 post-merge workflows passed, with three expected skips; no production writer ran.
  - Next: continue `BUSINESS-V2-14A` from merged main. `BETA-LIVE-MIGRATION-PARITY-001` remains a separate runtime/release blocker.

- **`BETA-BUSINESS-V2-001` — Business V2 completion through Phase 14**
  - Status: `IN_PROGRESS`
  - Requested scope and defaults: implement `BUSINESS-V2-10A4C4`, `BUSINESS-V2-10A4D`, `BUSINESS-V2-11`, `BUSINESS-V2-12`, `BUSINESS-V2-13`, and `BUSINESS-V2-14A`–`14D` as an unmerged draft stack. Only committed Player Store purchases may create new physical-goods sales after Phase 11; statements close on guarded server-owned payroll periods; IPOs are fixed-price primary issuances; legacy operating periods use a seven-day cadence and later policy changes affect only unopened periods.
  - Current C4 state: `IMPLEMENTED_NOT_MERGED`. Existing owner `feat/business-multicurrency-treasury-v1`, draft PR #678, exact implementation and verification source `46bfc611834dca4db3084d9dce8197c499d61fcd`, based on the C3 controller handoff `18fde31be5e1599c7d9a65d681b248fcb4756dc4`; certified C3 implementation identity `058162d7b9688809e885d9e6fe77ed42978c7a03`. C4 extends B2 account/FX and C0 funding authority to exact Player-or-Business ownership, atomically funds Business procurement, and exposes the locked authenticated Treasury and Store surfaces without a parallel wallet, ledger, balance, FX, funding, Store, or Inventory authority.
  - C4 implementation evidence: four forward migrations `20260831100000` through `20260831103000` generalize owner identity, add Business treasury FX, add funded procurement, and assert the resulting invariants. No new game-scoped table was added, so no purge-registry migration exists. The eight authenticated routes cover Treasury read/account opening, FX quote/standard/instant/cancel, and funded Business Store quote/purchase. Permanent C4 run `33351825999` passed source/authority job `99366568097`, replay/database/concurrency job `99366567927`, and desktop/mobile Chromium job `99366568058`. All 31 PR-triggered workflows passed on the exact implementation SHA; connected Marketplace run `33351825985` proves quote, settlement, replay, dispute, cancellation, unauthenticated rejection, UUID privacy, and console/page cleanliness after the retained capability-manifest repair.
  - Current 10A.4D state: `IMPLEMENTED_NOT_MERGED`. Owner branch `feat/business-player-store-fx-final-v2`, draft PR #679, exact implementation and verification source `e0bebfc3e774f2c7fa6e91d88b899862e7ca1d8b`, based on exact C4F controller `51ffd008ed84f6a9acd029c8941b3f9b40733735`. Both Player Edge roots compose one funded Store mutation repository for seeded/NPC and Business sellers; ordered one-to-three Checking intent uses a server-derived final remainder; full immutable C0/B2 funding evidence and seller proceeds survive quote, settlement, replay, receipt reread, refresh recovery, and withdrawal ordering; same-currency wallet/THD assumptions are retired. The forward migration is `20260831103001_business_player_store_fx_final_v2.sql`, generated with the Supabase CLI and reconciled to the first strictly later repository version after the CLI's UTC version sorted behind C4's reserved versions. It adds bounded existing-row evidence and function changes only: no predecessor migration, new table, RLS policy, direct DML grant, C0/B2 composer, or purge registry changed.
  - 10A.4D implementation evidence: permanent run `33377788370` passed connected job `99443203694`, source/authority job `99443203926`, zero-to-head replay/database/concurrency job `99443204017`, and desktop/mobile Chromium/accessibility job `99443204056`. Retained Store cutover run `33377788283` passed connected Buyer/seller two-game job `99443148484` plus serial/race/isolation, replay/lint, Edge, Player, authority, security, and repository lanes. All 35 pull-request-triggered workflows returned for `e0bebfc3e774f2c7fa6e91d88b899862e7ca1d8b` completed successfully. Evidence proves seeded and Business all-foreign funding, exact seller proceeds, Inventory/custody settlement, immutable receipt reread, replay/conflict, purchase-versus-withdrawal ordering, two-game isolation, UUID/privacy denial, responsive/a11y behavior, and clean console/page state.
  - Current Phase 11 state: `IMPLEMENTED_NOT_MERGED`. Owner branch `refactor/business-store-sales-convergence-v2` was reconciled into merged `main` baseline `9dc7906bb278b7eee9ceef6d3624fb088a6b5a97`; the certified implementation identity is `3cbca309e1e3c55e9b933803d304d2c5cc96f071`. New physical-goods revenue, COGS, gross margin, and gross-receipts tax derive from committed Store receipts; guarded operating/payroll periods and the internal operations worker remain server-authoritative.
  - Current Phase 12 state: `BLOCKED` for live parity only; repository implementation is merged and verified. PR #681 merged as `b09905c066016ae1523d2f0a991e86f2f60c70db`, retaining certified implementation `76539c5cfcff612a322963e303e727e6edc7f7ed`. Post-merge checks passed; no deployment/live SQL occurred.
  - Current Phase 13 state: `BLOCKED` for live parity only; repository implementation is merged and verified. PR #682 merged as `4a9674ed9ca0565c828093fe100aa658ef4faaa6` from candidate `6ba9b3d6e53eb00052c220bea2a04191c9fb913f`: 51 workflows, 93 checks and Vercel status passed; one staging-only workflow and 13 non-applicable checks skipped. Business/World journeys and both 30/40-player load profiles passed. Application/migrations match original certification `949da9f91990eeeee2cd815541446a5b64130664`. Evidence: `docs/operations/evidence/business-phase12-phase13-merge-2026-09-18.json`.
  - Successor owner sequence: `feat/business-player-store-fx-final-v2` → `refactor/business-store-sales-convergence-v2` → `feat/player-business-workspace-v2` → `feat/admin-business-supervision-v2` → `feat/business-financial-reporting-v2` → `feat/business-common-equity-v2` → `feat/business-ipo-issuance-v2` → `feat/business-financial-market-integration-v2`. A successor branch may be created only from its exact green predecessor handoff, and each gets one bounded draft PR.
  - Stack reconciliation (2026-09-18): older Business stack PRs #648 and #654–#680 are closed; main already contained Phase 11. PRs #681/#682 are merged. Unrelated owners #668 (ARCH-100F), #624 (Player CSS), #620 (dependencies) remain open. Old draft-stack integration text is historical, not authority to reopen or merge closed PRs.
  - Completion and release boundary: the owner authorized Phase 12/13 merges and Phase 14 start on 2026-09-18. No deployment, scheduler/secret change or live SQL/data mutation is authorized. `BETA-LIVE-MIGRATION-PARITY-001` remains a release/runtime blocker; repository merge alone does not mean `VERIFIED_COMPLETE`.
  - Integration reconciliation (2026-09-18): PR #648 is closed and its old integration instruction is superseded. Phase 14 starts from verified merged main; do not reopen the retired controller. Preserve one owner per bounded successor and rerun the applicable zero-to-head/application/security/browser matrix before any later merge.
  - Separate architecture gates: `ARCH-100G0`, `ARCH-100L2`–`L5`, `ARCH-100I2A/B`, `ARCH-202A/B/C`, `ARCH-207A-D`, and `ARCH-300` remain post-merge gates. This stack must preserve their context and ownership invariants but cannot claim their merged-source completion.
  - Phase 13 continuation authorized 2026-09-17: finish `BUSINESS-V2-13A` through `BUSINESS-V2-13F` on existing draft PR #682 without merging or deploying; Phase 14 remains outside this authorization. Initial head `4b0158e56394d373942dbcdfcf913a7a2de893e5` had seven failed workflows caused by generated-inventory drift and missing PR-bound retained authority. Repair `435f04775f9a5fd229ada71afc9054f3cc90a10f` passed all 30 workflows then triggered, with exact path allowlists and the unchanged 100-file size-debt ceiling.
  - Phase 13 full certification 2026-09-17: 13B–D reuse unchanged canonical calculation bodies and retain Player ownership; 13E explicitly adds no intervention. Two CLI-generated forward read-function migrations and service-only `read_admin_business_supervision_v2(uuid,uuid,text)` are included; no live application is authorized. Permanent run `35282299961` passed source job `105406775616`, database job `105406775583` and browser job `105406775177`. Closed-history readiness, retry focus, exact capitalization and UUID privacy fixes are verified. All retained source/Edge/database/browser/economic gates passed at `949da9f91990eeeee2cd815541446a5b64130664`.
  - Phase 14A–D is now repository-certified on draft PRs #684–#687. The remaining boundary is owner-authorized ordered integration and live parity/runtime release evidence. No further Phase 14 repository checkpoint remains.

- **`ARCH-100` — Canonical request/application context**
  - Status: `IN_PROGRESS`
  - Owner branches and exact bases: bounded Player projection tranche `refactor/request-application-context-v1` merged by PR #633 as `baca5cfdd5934f1f85b3666d4e85bfac30244060`. Admin projection tranche `refactor/admin-request-application-context-v1` reconciled through `7a5542afa9ca50d83b6e045b7d4ea8993477645b` and merged by PR #635 as `c4710c0a7fc51773f8a1fb0050e6b2d1b785d3aa`. Player Inventory propagation branch `refactor/player-inventory-context-propagation-v1` merged by PR #637 as `0e35bbab51e9459ceb83246e6d72ea1123b29283`. Admin Game Sessions propagation branch `refactor/admin-game-sessions-context-propagation-v1` was created from that exact verified merge, accepted at head `b19da03f4838d39a7bfa91ec5e36474177d56e3d`, and merged by PR #638 as `651e607c0f63f79532c9f06ee74b705622ee7819`; all `ARCH-100D` gates are verified and its owner branch is retired locally and remotely. Residual-classification owner `refactor/context-propagation-residual-classification-v1` was created from that exact verified merge, accepted at head `67b8db18013a82338001782fc604270ad25c312b`, and merged by PR #639 as `83c3177ed112cc93bedae2d2d4018554c4c40a83`; all `ARCH-100E` gates are verified and its owner branch is retired locally and remotely. The next serialized owner is `ARCH-100F` on `refactor/multi-game-bootstrap-context-hydration-v1`, which must be created only after this completion record merges and from that exact current `main`.
  - Scope and beta impact: merged Player and Admin projection tranches create distinct immutable contexts only after server-side actor/game scope derivation and inside their reviewed security/rate boundaries; the Player limiter receives the exact derived context, while Admin/shared Staff universal guards precede creation and later scope-dependent domain limiters must receive that context. Verified `ARCH-100C` preserves the exact Player context through Inventory read/redemption use-case and repository seams, projecting game/player scalars only inside Supabase persistence adapters. Verified `ARCH-100D` preserves the exact existing Admin context through Game Sessions join-code/settings seams and creates equivalent server-derived Staff contexts for shared Classroom/Staff handlers, with persistence projection confined to narrow adapters and server request identity kept separate from mutation/idempotency identity. External methods, routes, auth, permissions/AAL, payloads, errors, idempotency, UI, schema, RPCs, migrations and deployments remain unchanged. `ARCH-100E` audits the known/reviewed residual scalarized paths and assigns dependency-ordered, collision-bounded owners before further implementation; `ARCH-100X` must reconcile a deterministic root-to-handler inventory, so this classification cannot itself close `ARCH-100`.
  - Collision audit: the final `ARCH-100E` audit before PR #639 merged covered open PRs #619 (root dependencies), #620 (workflows), #624 (Player Terminal CSS/realtime/browser tests and Player authority manifest) and #626 (Business browser acceptance and the same manifest); none owned residual context classification. #620 remains an acceptance-control-plane collision, and #624/#626 constrain any later Player browser/authority-manifest scope. Never merge/cherry-pick stale exact-path donors `wip/codex-production-slices-2026-08-04`, `automation/admin-v2-ux-root-apply-20260814`, `agent/market-minute-replay-v1`, the two PR501 banking release aliases if `readModels.ts` is touched, `feat/story-narrative-convergence-v1`, or `fix/contract-story-flag-rewards-v1` where Contract rewards overlap. Classification changed only the new owner map and both roadmaps; each implementation owner must re-audit exact files before editing. Root manifests, workflows, UI/browser files, authority manifests, create-game/provisioning, migrations/RPC SQL and deployment files remain excluded unless a later explicitly bounded owner proves they are required.
  - Implementation files: merged projection files include the shared context, Player scope/rate-limit composition, both Player roots, Admin permission/context factory, Admin root/Inventory adapter, focused tests/contracts, generated inventory and both roadmaps. `ARCH-100C` adds the Inventory type-only context port and redemption service; updates Inventory read/redemption contracts, handlers, read service, both Supabase repositories, focused tests, full-lifecycle test and the Player Inventory test registration. `ARCH-100D` adds the Game Sessions Staff context and neutral mutation ports, shared Staff context factory, settings read use case, three narrow Supabase adapters and two-root composition contract; updates join-code/settings applications and shared handlers; forwards the exact existing Admin context through `index.ts`, read/local/compatibility routers and legacy settings mapping; registers every new test; and refreshes the deterministic inventory to 1,040 source files with all debt limits flat. Classroom and Staff roots remain byte-unchanged and are now both characterized. `ARCH-100E` adds `docs/architecture/econovaria-context-propagation-owner-map-v1.md`, audited against exact main/tree `651e607c0f63f79532c9f06ee74b705622ee7819`/`f784e1d585db8b2e4337f50558b23442bbee8e56`; it defines the canonical invariants, ten boundary classes, reviewed Admin/Staff/Player residual candidate inventory, collision ledger, per-owner gates, mandatory Players/Contracts/Economy/Stocks subowners, and the strict `ARCH-100F` through `ARCH-100X` sequence without changing runtime code. `ARCH-100X` must still generate an exact-main root-to-handler artifact with zero unassigned live edges before the parent can close.
  - Pull requests and commit SHAs: Player implementation `cb356a8fb6e6d6f5b826a437b60d33c6121d1429`; final PR #633 head `4a1cbd6322853ae46cdc7ddfcd186c42d667544a`; merge `baca5cfdd5934f1f85b3666d4e85bfac30244060`. Admin implementation `393e93de039c36e1180e205d381a2ad32c9d7e47`; final PR #635 head `c4771765699f26c456883b9be30cd00cc06b249d`; merge `c4710c0a7fc51773f8a1fb0050e6b2d1b785d3aa`. `ARCH-100C` implementation `15aa42cc5dc128b19238f23d0c91201c744fbc1a`; PR #637 accepted head `1fb86699178ed65567dbf5b28263b053a436f0b1`; merge `0e35bbab51e9459ceb83246e6d72ea1123b29283`; tree `376a030061946a424df5406c8ba52bdede875c5a`. `ARCH-100D` implementation `a59e5caaddf995499795481c897c756395e34916`; PR #638 accepted head `b19da03f4838d39a7bfa91ec5e36474177d56e3d`; merge `651e607c0f63f79532c9f06ee74b705622ee7819`; accepted-head and merge tree `f784e1d585db8b2e4337f50558b23442bbee8e56`. `ARCH-100E` implementation `977db267704f4ab7c6dc6bf7a84ef62a923efd82`; PR #639 accepted head `67b8db18013a82338001782fc604270ad25c312b`; merge `83c3177ed112cc93bedae2d2d4018554c4c40a83`; accepted-head and merge tree `07440291f6b46011b587dad7346d02bc0c99710b`. Every accepted-head tree equals its merge tree.
  - Tests/runtime evidence: `ARCH-100C` local evidence passed Player Inventory 50/50; request-scope/privacy 22/22; Player security and both-root composition 43/43; adjacent Crafting 25/25, Store 14/14 and Marketplace 48/48; Crafting runtime 37/37; economic/store contracts 16/16; auth boundaries 16/16 plus 8/8; `typecheck:all`; separate Player API Deno check; full backend smoke including Admin API 160/160; complete root `npm test`; high-priority/architecture/legacy ratchets; deterministic 1,029-file inventory with every architecture debt limit flat; secret, JSON and diff checks. Its exact-head, exact-merge, deployment, parity, health and hygiene evidence remains recorded in the architecture roadmap. `ARCH-100D` local evidence passed Game Sessions 58/58 including both roots, Admin local mutations 80/80, Admin API 165/165, `typecheck:all`, explicit Staff root check, full backend smoke, full root `npm test`, Admin local UI/composition 15/15, game-session controls/lifecycle, auth boundaries 16/16 plus 8/8, high-priority/admin-contract/architecture/legacy ratchets, deterministic 1,040-file inventory with every v2 debt and zero-tolerance measurement flat, secret and diff checks. Thirty-two formatted touched/new TypeScript files pass; the two collision-sensitive legacy sources retain pre-existing whole-file format debt without broad reformatting. Independent review findings on dependency direction and two-root evidence were fixed and re-review found no remaining defect. Exact-head PR #638 evidence is 41 passes, 10 intended skips, no unresolved check and no review thread. All 17 merge-SHA workflows passed, including Edge inventory convergence `32083315746`, Production Git Release `32083315784`, CodeQL `32083315409` and downstream Vercel verification `32083500512`; staging/production Edge inventories and bounded live parity passed, `release/production` points exactly to the merge, canonical health reports the exact source commit with both session services healthy, and Branch Hygiene `32083315958` passed. `ARCH-100E` local documentation acceptance passed architecture, high-priority, legacy-runtime, secret and diff gates with the deterministic 1,040-file inventory and every debt/zero-tolerance value unchanged; three independent audit/re-review lanes reported no remaining blocker. Runtime/typecheck, migration, staging, and connected-system suites were locally inapplicable because no source/runtime contract changed. Exact-head PR #639 had 10 successful checks, one successful Vercel status, two intended skips, no unresolved check and no review thread. All eight exact-source merge workflows passed, including Production Git Release `32085873306`, CodeQL `32085873137`, Repository Quality `32085873330` and downstream Vercel verification `32085966812`; bounded live parity passed with zero unapproved differences, `release/production` points exactly to the merge, and canonical production health reports the exact source commit with both session services healthy at HTTP 200. Edge attestation was correctly inapplicable because no Edge source changed. Branch Hygiene `32085874369` passed and the owner branch is absent locally and remotely. No schema, migration, RPC signature, route, response, UI, deployment configuration, economic-write, idempotency or runtime dependency changed; tranche-specific connected staging remained inapplicable.
  - Completion boundary: `IN_PROGRESS`; `ARCH-100A`, `ARCH-100B`, `ARCH-100C`, `ARCH-100D` and `ARCH-100E` are `VERIFIED_COMPLETE`. The dependency-serialized implementation queue is `ARCH-100F` multi-game bootstrap, mandatory `ARCH-100G1`–`G3` Players, then Attendance, Store, mandatory `J1`–`J3` Contracts, mandatory `K1`–`K2` Economy, Business Banking, mandatory `M1`–`M2` Stocks/market, Marketplace, World, Messaging, Progression, Notifications, Storylines, Crafting, lifecycle/post-create provisioning, Dashboard and audit owners, ending with `ARCH-100X` deterministic closure. None of those runtime paths is claimed implemented by the classification audit. Next exact item: `ARCH-100F` on `refactor/multi-game-bootstrap-context-hydration-v1`, which must be created only after this completion record merges and from that exact current `main`. `ARCH-100` cannot close and `ARCH-101` cannot start until every resulting owner is merged and verified.

- **`ARCH-001` — Architecture ratchets before large moves**
  - Status: `VERIFIED_COMPLETE`
  - Owner branch and baseline: `chore/architecture-ratchets-v1`, created from merged ARCH-000 main `e40cb5b05c913fb52f402e2f7171f8b7ee69ad63` and reconciled with current main `7ecc9e018f6ee82ef4f4eae56a824e719481c3fd`.
  - Scope and beta impact: compose the existing architecture and legacy-runtime audits with a checked v2 baseline that permits measured debt only to stay flat or decrease. Zero-tolerance checks cover direct browser database access, out-of-owner balance/Inventory mutation, unscoped live-simulation persistence and retired browser markers. No product behavior, UI, route, RPC, migration, database or deployment change.
  - Collision audit: open PRs #619, #620, #624 and #626 do not own the ratchet files; no in-flight Player/runtime file is modified.
  - Implementation files: `scripts/architecture/architecture-ratchet-v2.mjs`, `scripts/architecture/architecture-ratchet-v2-baseline.json`, `package.json`, architecture dependency documentation and both authoritative roadmaps.
  - Pull request and commit SHA: PR #631; implementation commit `a19b8b0758cf174dfb8205676b1bd4d5175c326d`; merge commit `92a818dfd719d1dcf1407c39bb6904f5d9b78077`.
  - Tests/evidence: `npm run audit:architecture`, `npm run audit:high-priority-boundaries`, `npm run audit:legacy-runtime`, syntax/JSON/diff/secret checks and every required PR check passed. Release-only jobs were skipped by their intended conditions; transient external action-download/Admin browser failures passed unchanged on rerun. Runtime/staging evidence is inapplicable to read-only architecture tooling.
  - Completion boundary: merged into `main` with required evidence; no unresolved blocker. Next exact item: `ARCH-100`.

- **`ARCH-000` — Current architecture inventory**
  - Status: `VERIFIED_COMPLETE`
  - Owner branch and baseline: `agent/architecture-hardening-roadmap-v2`, audited against fetched `origin/main` `72cefb73a0038aa2bc24261d63e70c113cb7c24c`.
  - Scope and beta impact: Phase 0 measured baseline for the architecture-hardening program. Documentation and read-only inventory tooling only; no product behavior, accepted UI, API, route, RPC, migration, database, deployment or production-state change.
  - Collision audit: open PRs #619, #620, #624 and #626 were inspected. None owns ARCH-000; #624's Player CSS/realtime work is treated as in-flight and was not modified.
  - Implementation files: `docs/architecture/econovaria-domain-ownership-v2.md`, `docs/architecture/econovaria-dependency-map-v2.md`, `docs/architecture/inventories/econovaria-architecture-inventory-v2.json`, `scripts/architecture/build-architecture-inventory.mjs`, and the `audit:architecture-inventory` package script.
  - Measured baseline: 26 domains, 24 Edge entrypoints, 58 handler candidates, 168 cross-domain deep imports, 100 persistence-call candidates outside approved infrastructure paths, 27 browser shim/observer files, 209 compatibility-marker candidates, six scheduler/worker entrypoints, 100 source files at or above 500 lines, and 123 capability-like strings. Lexical candidates require characterization before refactoring or deletion.
  - Tests and evidence: deterministic `audit:architecture-inventory`; `audit:high-priority-boundaries` (80 checks); `audit:architecture` (7 broad fetch assignments, 1 scoped assignment, 11 MutationObservers); `audit:legacy-runtime` (8 groups, 15 runtimes, 2 credential records); Node syntax; JSON parse; `git diff --check`; and tracked/untracked changed-file secret scans all pass. Runtime and staging evidence are inapplicable because the tranche is documentation/read-only tooling.
  - Pull request and commit SHA: PR #629; implementation commit `71bb4911f8a9e256549629ff74750a65cf26de46`.
  - Completion basis: PR #629 passed its full suite and merged into `main` as `e40cb5b05c913fb52f402e2f7171f8b7ee69ad63`. The transient Business Banking action-download failure passed unchanged on rerun. No runtime/staging evidence was required for documentation/read-only tooling.
  - Unresolved blocker: none.
  - Next exact roadmap item: `ARCH-001`.

- **`BETA-ADMIN-UI-V2-003` — Market Management native v2 migration**
  - Status: `IMPLEMENTED_NOT_MERGED`
  - Owner branch and exact base: `refactor/admin-ui-v2-market-management-v1`, created from fetched `origin/main` at `9eb2277e6e16d628c0da2595d05bf99a121002a9` after Phase 2 merged.
  - Scope: migrate only the financial `Market` destination into the source-owned Admin v2 shell so Overview, Store, and Market are the only native v2 routes. Keep `Marketplace` separate with its existing `marketplace.moderate` planned disposition, retain World Management as first-class and planned, and leave every other route unchanged.
  - Contract boundary: audit the merged Admin and Player financial-Market contracts before implementation, then consume only existing same-origin HttpOnly-BFF reads and any proven `market.manage` mutations. Do not add a Backend endpoint, database or Supabase change, Player trading behavior, legacy Admin behavior, browser bearer token, or unsupported control.
  - Presentation and evidence boundary: present authoritative exchange/session availability when exposed and an explicit unavailable state when the current read contract omits it; implement the instrument directory and supported detail/history presentation, all shared v2 data states, permission failure, responsive table/cards and drawer behavior, safe errors, and focused source/built browser evidence at the seven required viewports. Preserve Overview, Store, Store-media, authentication, Player Market/order/Portfolio, build, syntax, architecture, secret, and diff regressions relative to the exact base.
  - Delivery boundary: commit and publish one bounded draft PR into `main`, allow only the automatic Git-connected Vercel preview, and stop after the draft PR and preview are reviewable. No merge, manual deployment, staging or production mutation, production promotion, Marketplace work, or another route migration is authorized.
  - Next exact roadmap item: complete this same Phase 3 contract inventory, implementation, review evidence, draft PR, and automatic preview; do not begin another route automatically.

- **`BETA-ADMIN-UI-V2-002` — Admin v2 Store Management and canonical Store media**
  - Status: `VERIFIED_COMPLETE`
  - Merge reconciliation: [PR #505](https://github.com/kohnerbouchard-star/Student-Profile/pull/505) merged into `main` as merge commit `9eb2277e6e16d628c0da2595d05bf99a121002a9`, preserving final published head `7d554bae1edc1448ace8603a8b40ce6829b77ee1`. The implementation was based on `60334c2e92c6396a15c61fdd7b3552b055aee271`, the canonical Phase 1 tree.
  - Scope: make Store the second native source-owned Admin v2 route while retaining Overview; implement authoritative Store read/create/update/archive presentation, responsive table/cards, search and supported filters, all six data states, and accessible create/edit/archive dialogs using the existing shared v2 primitives. World Management remains a first-class left-navigation item and every other route retains its Phase 1 legacy or planned disposition.
  - API and security boundary: consume only the existing same-origin HttpOnly-BFF routes `GET /api/admin/games/:gameId/store/items?include=stock,prices,purchaseStats`, `POST /api/admin/games/:gameId/store/items`, `PATCH /api/admin/games/:gameId/store/items/:itemId`, and `DELETE /api/admin/games/:gameId/store/items/:itemId`. Preserve browser-session CSRF, server-authoritative AAL2, `store.manage`, game ownership, rate limiting, audit identity, and payload-bound idempotency. Do not expose or mock a browser-readable Staff bearer token, patch global `fetch`, or render raw Backend/private-ID details.
  - Persisted-field boundary: the existing create contract persists an authoritative generated `itemKey`; Phase 2 does not expose it as a direct control. Create/edit support `name`, `description`, `category`, `price`, `currencyCode`, `stockQuantity`, `status`, `visibility`, and `sortOrder`. Archive uses the existing soft-archive `DELETE` contract. Artwork/upload, unlimited stock, country stock, restock, fulfillment, and usage-configuration controls are intentionally absent because those contracts are not persisted by the current Store DTO. Purchase statistics may remain read-only.
  - Canonical media: add shared resolver `player-terminal/assets/store-item-media.mjs` and graphical fallback `player-terminal/assets/store-item-placeholder.svg`. Valid seeded and allow-listed catalog keys resolve only to repository-owned artwork; unsupported, custom, malformed, private, or failed media resolves to the branded placeholder. The DTO has no custom-media field, and existing multipart Store image requests are rejected until an approved storage/media policy exists, so Phase 2 contains no fake upload affordance and never trusts an item-supplied image URL.
  - Player compatibility boundary: adapt only the existing Player Store artwork helper and Store media error path to the canonical resolver so seeded/catalog artwork remains visually identical and failures use the same placeholder. Keep legacy Marketplace's repository-local SVG compatibility separate. Do not change Player purchase, stock, price, inventory, authentication, or route behavior.
  - Implementation ownership: shared media under `player-terminal/assets/`, where the standalone Player static root can serve it and Admin v2 can import the same module; Store route modules under `admin/v2/src/routes/store/`; Store-only CSS under `admin/v2/styles/routes/store.css`; bounded composition, registry, media-primitive, API-client, and BFF-transport extensions under the existing `admin/v2/` architecture; focused tests under `scripts/`; Phase 2 evidence under `docs/operations/evidence/admin-ui-v2-store/`; architecture and PR records at `docs/architecture/admin-ui-v2-store-phase2.md` and `docs/pull-requests/admin-ui-v2-store-phase2.md`. No Backend, database, migration, Supabase, authentication, rate-limit, production-config, or legacy Admin file belongs to this change.
  - Legacy retention and cutover: legacy Store remains present and independently operable under `/admin`; Phase 2 deletes no legacy file. Retiring Store-specific legacy code requires merged-main parity, passing API/session/mutation/media/focus/responsive/browser evidence, proof that each proposed file has no remaining consumer, and separate product-owner cutover authorization.
  - Required evidence: Admin v2 unit/API/media tests; source and configured built-dist browser suites; configured Vercel build and deployment contract; authentication and session boundaries; create/edit/archive success and safe-failure scenarios; keyboard/dialog focus; zero document horizontal overflow; zero/one/50-plus items; correct MIME and no missing resource, unexpected console error, CSP violation, or Trusted Types warning; JavaScript syntax; `git diff --check`; changed-file secret scan; and no new failure relative to exact base. Required screenshots cover 1440×900, 1280×720, 1024×768, 768×1024, 390×844, 320×568, and 1024×540 plus representative states, dialogs, real artwork, and placeholder artwork.
  - Review record: implementation commit `3663c2f906778c98469d5d68b4abcda72f34f28c` and standalone Player static-root correction `e52db6e37f3430c4cfd47a194b9065dbae67b718` merged through PR #505; final scope is 69 files, 5,658 additions, and 113 deletions relative to exact base. Admin v2 unit/API/media, Store source and built-dist browser, Overview regression, auth/request-auth, configured build/deployment contract, Store/Player artwork, focused Player, standalone Progression, enabled-button safety, Multiplayer/Load, runtime cutover, syntax, asset, diff, and secret checks passed. Required Store evidence is committed under `docs/operations/evidence/admin-ui-v2-store/`. The inherited architecture, legacy scroll, v606 hash, lifecycle runtime-style, and cross-cutting authority-gate failures remained separately classified and were not weakened.
  - Runtime and completion evidence: automatic Git-connected deployment `dpl_GedMQVWxHy2NXij8p6cqFzBmH1Q3` built final head `7d554bae1edc1448ace8603a8b40ce6829b77ee1` and was Ready at `https://econovaria-qnjkn5kde-econovaria.vercel.app`; `/admin/v2.html#store` returned HTTP 200 with `text/html` and the expected Store CSS and v2 module. The initial standalone Player media-module path regression was fixed by `e52db6e37f3430c4cfd47a194b9065dbae67b718`, and no genuine Phase 2 product/browser regression remained at merge. No manual deployment or production promotion occurred.
  - Next exact roadmap item: `BETA-ADMIN-UI-V2-003`, financial Market Management as the third native v2 route while Marketplace remains separate and unmigrated.

- **`BETA-ADMIN-UI-V2-001` — Source-owned Admin v2 foundation and Overview reference migration**
  - Status: `VERIFIED_COMPLETE`
  - Merge reconciliation: [PR #502](https://github.com/kohnerbouchard-star/Student-Profile/pull/502) merged into `main` as squash commit `60334c2e92c6396a15c61fdd7b3552b055aee271`. Its tree is identical to the final published PR head `5776494b9cfde6902f839592b44692e22327a21a`; the remote feature branch was deleted after merge. The earlier implementation commits `3baed2ae36b9cbdf19fadab9b696f5b504d89eeb` and `462a15aaf8b9c292f786509b2fc52cf6cce3b425` remain provenance, while the squash commit is the immutable merged implementation.
  - Ownership reconciliation: Phase 1 is now owned by merged `main`, not an active feature branch. PR #498 / `fix/admin-modal-focus-order-v1` remains separately scoped to the legacy modal focus-order compatibility correction (`BETA-ADMIN-MODAL-A11Y-008`). The retained `frontend/admin-terminal-source-v1` branch remains a stale source-preservation donor, not active feature authority, and must not be merged wholesale.
  - Scope merged by PR #502: isolated source-owned vanilla-ESM Admin shell and component system; canonical eight-group, 18-route primary navigation with Market and Marketplace kept distinct and World Management first-class under `World`; accepted v606 visual language as the design baseline; authoritative Overview reads and all six data states; explicit legacy handoffs only for exact standalone destinations; neutral planned boundaries with no unrelated links for the remaining domains; shared tokens, dialog/drawer, data, form, table, error, permission, and accessibility primitives. Overview was the only native V2 route at Phase 1 completion.
  - Dependencies and beta impact: architecture, accessibility, resilience, and maintainability hardening only; no gameplay semantics, backend endpoint or authentication/authorization policy, Supabase configuration, BFF routing, database migration, or Player Terminal behavior changed. The separate legacy-only PR #498 does not own or alter this merged v2 architecture. Two inherited legacy required-check failures remain unchanged: `admin-shell-smoke.mjs` passes four source children but only 2/4 viewport assertions, and `admin-v606-full-drift-audit.mjs` expects obsolete `admin/css/page-shell.css` blob `c4df8ae6…` instead of current `a9644c2…`. They are separately owned legacy debt and are non-blocking for this V2 tranche; their assertions and accepted hash remain unchanged.
  - Security and data boundary: use a V2-scoped, read-only `/api/admin` HttpOnly-BFF transport without loading legacy `admin-auth.js` or patching global `fetch`; preserve server-derived game scope and permission grants; never render internal ownership UUIDs or raw backend, SQL, Supabase, stack, function, environment, or service-role details. Per product-owner classification, the unchanged legacy selected-game `?game=` contract may continue to carry its UUID during Phase 1. Its eight URL assertions remain active and are reported as expected legacy-contract privacy/hygiene exceptions, not Phase 1 failures. Public-handle design and coordinated URL cleanup are deferred to a separately owned tranche.
  - Implementation files: new static entry `admin/v2.html`; source tree `admin/v2/src/{main.js,app.js,api/**,core/**,components/**,routes/overview/**}`; scoped styling `admin/v2/styles/{reset,tokens,base,components,utilities}.css` and `admin/v2/styles/routes/overview.css`; focused tests `scripts/admin-v2-{unit.test,browser-smoke,browser-fixture-server}.mjs`; test command registration in `package.json`; architecture and exhaustive legacy/deletion inventory `docs/architecture/admin-ui-v2-phase1.md`; draft PR handoff `docs/pull-requests/admin-ui-v2-phase1.md`; local evidence under `docs/operations/evidence/admin-ui-v2-phase1/`; and this ledger entry. Existing legacy Admin and Player Terminal files remain unchanged.
  - Migrations, routes, and RPCs: no database migration, RPC, Backend, BFF rewrite, or API behavior change. New static UI route `/admin/v2.html` owns the migrated Overview reference implementation; existing `/admin/` remains the independently owned legacy runtime during Phase 1. Players, Attendance, financial Market, Contracts, Store, Settings, and Logs retain explicit legacy handoffs. Banking, Loans, Business, Crafting, Marketplace, Inventory, World Management, News & Events, Messages, and Progression render planned states with no legacy link. World Management remains first-class; Market never hands off to Marketplace and Marketplace never hands off to Market.
  - Local tests and workflow results: `npm run test:admin-v2` passes 6/6. Source and built-dist browser audits each exit zero with 42 passing Phase 1 checks, eight preserved expected legacy-contract exceptions, and zero failures, covering the exact taxonomy and permissions, all seven legacy handoffs, all ten planned boundaries, planned-route permission denial, Market/Marketplace independence, direct unauthenticated navigation, missing game, expired/revoked/security-version-invalid sessions, AAL2, API 401, permission 403, 429 with seven-second retry guidance, retryable 503, valid reload, no pre-validation data flash, safe DOM errors, and all required viewports/states. The same eight UUID-free URL predicates remain factually false and separately visible as deferred debt. JavaScript/CSS MIME, missing/failed static requests, CSP/Trusted Types warnings, and unexpected console/page errors are clean. `npm run test:auth-boundaries` passes 16/16 plus 8/8; `npm run audit:admin-contracts` passes with all 88 routes covered. `npm run audit:architecture` retains the current-main-identical `MutationObserver` 12-versus-11 failure and introduces no new regression. The initial implementation commit is `3baed2ae36b9cbdf19fadab9b696f5b504d89eeb`; the bounded taxonomy correction commit is `462a15aaf8b9c292f786509b2fc52cf6cce3b425`. Existing failed workflows remain current-main/base-identical legacy debt; no V2 file owns a first failure.
  - Local screenshot evidence: `docs/operations/evidence/admin-ui-v2-phase1/admin-v2-browser-results.json`; ready captures at 1440×900, 1280×720, 1024×768, 768×1024, 390×844, and 320×568; 1024×540 short-desktop capture; and loading, stale, failed, permission-denied, and empty captures at 1280×720. These are sanitized fixture captures committed with the implementation, not authenticated deployed-state screenshots.
  - Runtime and preview evidence: the Git-connected Vercel preview was Ready at https://econovaria-git-refactor-admin-ui-v2-overview-a22902-econovaria.vercel.app. Direct navigation and reload of `/admin/v2.html` each returned initial HTML HTTP 200. The preview inventory returned HTTP 200 with the correct MIME type for all 48 intended files; a browser direct-load recorded all 40 V2 JavaScript and CSS resources with no missing-resource, CSP, or Trusted Types failure. The session-dependent preview probe exposed an inherited staging-preview CORS condition reproduced against the unrelated PR #501 legacy Admin preview, not a new Phase 1 regression. Production deployment was not a Phase 1 acceptance requirement and was neither performed nor authorized.
  - Completion basis: the implementation is merged into `main`; the Phase 1 unit, source-browser, built-dist-browser, session/authentication, resource, MIME, CSP, Trusted Types, syntax, and diff checks documented above passed against the accepted tree; the required local screenshots and Git-connected preview evidence exist; and no new V2-owned regression was found relative to its audited base. The separately recorded current-main-identical legacy ratchets and accepted selected-game URL exception do not change this item's verified state.
  - Deferred debt, not Phase 1 blockers: public-handle design and UUID URL cleanup; the inherited shell assertions and stale accepted v606 hash; and native-dialog migration. None may be silently deleted, weakened, or updated merely to make a later tranche pass.
  - Next exact roadmap item: `BETA-ADMIN-UI-V2-002`, Store Management plus canonical shared Store artwork/media resolution. After it is separately verified, the approved sequence remains World Management native; Players and Attendance; Contracts; Marketplace, Settings, and Logs; then legacy deletion. The other planned taxonomy entries are not implemented by their presence in navigation.

- **`BETA-PROD-ADMIN-WIRING-006` — Systemic Admin API local-handler correction**
  - Status: `RE_AUDIT_REQUIRED`
  - Ownership reconciliation: former owner branch `fix/admin-join-code-read-isolated`; implementation commit `d31c074b90b53fb81fc93b73afafcff8eb9df751` was merged by PR #496 as `31e1958abc063a8d19cf8e00a9b499623d5b4532`. The prior `IMPLEMENTED_NOT_MERGED` statement became stale when PR #496 merged.
  - Exact base: fetched `origin/main` at `f2694e40bac39b4ceb20951d88dddd38c6a9270a` contains the merge and later Admin/Player corrections. Merge presence alone does not satisfy this item's database-runtime, workflow, or staging evidence requirements.
  - Scope: replace synchronous `admin-api` to `classroom-api` calls for Admin player creation, Store item create/update/delete, Contract create/draft/schedule/publish, manual and scanner attendance recording, game-settings updates, and join-code read/rotation with shared local application handlers.
  - Security boundary: preserve one browser-BFF CSRF/network evaluation and one Admin authentication, AAL2, permission, owner-scope, user-action rate-limit, and request/audit-identity evaluation; do not accept browser forwarding headers as an identity source or pass a generic service-role bearer credential between functions.
  - Persistence boundary: require stable payload-bound idempotency for every scoped mutation; replay the original result for the same key and payload, reject key reuse with a different payload, and keep critical mutation, audit, and idempotency completion in one database transaction.
  - Architecture implemented: the Admin security guard performs the single authentication, AAL2, CSRF, permission, owner-scope, user-action rate-limit, and request/audit identity evaluation before `localGameMutations.ts` invokes shared Player, attendance, Store, Contract, settings, and join-code application modules. The Classroom entrypoint now reuses the same modules. A proxy deny-list prevents every scoped operation, including join-code GET, from reaching `fetchClassroom`. Compatibility actions are bound to their exact URL resource and method so a body operation cannot cross the permission domain.
  - Pull request and implementation commit: PR #496, merged as `31e1958abc063a8d19cf8e00a9b499623d5b4532`; implementation `d31c074b90b53fb81fc93b73afafcff8eb9df751`.
  - Implementation files: shared handlers under `backend/src/domains/{players,attendance,store,contracts,game-sessions}/application`; shared idempotency adapter `backend/src/platform/supabase/adminMutation.ts`; Admin dispatcher and compatibility boundary under `backend/supabase/functions/admin-api`; retained Classroom HTTP adapters under each domain `api` module; canonical BFF/web-session/local-gateway idempotency and trusted-IP handling; focused UI retry and response-projection corrections in the Admin terminal.
  - Migration: `20260805023228_admin_local_application_mutations_v1.sql` adds private, forced-RLS `admin_mutation_requests`, private begin/complete helpers, and service-role-only public RPCs `admin_read_mutation_replay_v1`, `admin_create_player_v1`, `admin_archive_player_v1`, `admin_mutate_store_item_v1`, `admin_mutate_contract_v1`, `admin_record_attendance_v1`, `admin_update_game_settings_v1`, and `admin_rotate_game_join_code_v1`. Each critical write, Staff audit entry, and idempotency completion occurs in one Postgres function transaction; no migration was applied to a database.
  - Remaining Admin-to-Classroom calls: Contract progress read; Player access-code reset; Contract submission decision/review; and Contract reward issue. They are distinct read/review/credential use cases outside this item, and the affected local-operation proxy guard rejects their use for this scope.
  - Validation: under repository-pinned Node `22.23.1`, npm `10.9.8`, Supabase CLI `2.109.1`, and the frozen Deno locks: `npm run test:admin-local-mutations` 76/76; `npm run test:admin-api` 139/139; `npm run test:game-sessions` 35/35; focused Classroom handler tests 34/34; `npm run test:admin-local-mutation-ui` 13/13; proxy/auth/gateway contracts 26/26; `npm run test:auth-boundaries` 16/16 plus 8/8; complete `npm --prefix backend run typecheck:all`; web-session Deno check; focused `deno lint` and `deno fmt --check`; migration and architecture audits; Admin game-session control smoke; changed-file credential scan; and `git diff --check` all pass.
  - Validation limitations: the Docker-backed local Supabase replay starts successfully but the existing migration chain stops at unchanged `20260721143000_harden_marketplace_legacy_projection_conflicts_v1.sql` with `MARKETPLACE_LEGACY_FUNCTION_BODY_UNRECOGNIZED:create_marketplace_listing_projection_legacy_v2`, before reaching this item's migration; database lint and the new RPC runtime replay therefore remain blocked. Repository-wide `npm test` stops on the unchanged stock-runner JWT configuration ratchet. Full backend smoke is additionally affected by a POSIX-only inline environment assignment on Windows and unchanged historical rate-limit, Inventory, Progression, Messaging, and economic-ledger migration text-contract failures. The affected Admin mutation, security-order, UI, typecheck, migration-static-contract, and architecture suites are green.
  - Product-contract decisions still required: secure server-generated one-time Player ID/Access Code delivery versus the established explicit-credential API; Store image storage, validation, access, and retention policy; representation of unlimited/country stock and persistence of UI-only item type/restock/fulfillment/usage fields; whether idempotency equality means the normalized business command or literal raw request bytes. Manual correction, Store archive, and publish-existing-draft backend routes are implemented but currently have no active v606 terminal controls.
  - Runtime evidence: the implementation is merged into `main`, but the recorded Docker replay still stops before this migration and the ledger contains no authorized staging/runtime proof for these local mutation paths. The item must not be marked `VERIFIED_COMPLETE` until the current merged head is re-audited and the missing evidence exists.
  - Next exact roadmap item: re-audit merged PR #496 against current `main`, repair the pre-existing migration replay blocker, run database lint and runtime replay/conflict/rollback probes, resolve the recorded product-contract decisions, and obtain any required authorized staging evidence before considering `VERIFIED_COMPLETE`.

- **`BETA-PROD-ADMIN-LOGIN-004` — Production Admin login trusted-header reconciliation**
  - Status: `VERIFIED_COMPLETE`
  - Repair pull request: PR #412, merged as `9350e5a8e3716779561db6432f8c11e345fa65c9`.
  - Regression-guard pull request: PR #414, merged as `2785cbf8`.
  - Root cause: production `web-session-api` version 15 returned `503 staff_login_unavailable` before emitting its first PostgREST or Auth request. Dependency-isolation run `30444485873` proved direct Auth and all three login-throttle RPCs were healthy. The remaining fail-closed boundary was the configured `x-real-ip` metadata, which was unavailable after the Supabase gateway hop.
  - Production repair: protected run `30446280999` verified the same `cf-connecting-ip` binding on staging, applied it to production project `cgiukdjwicykrmtkhudh`, preserved the healthy function, and made no database, application-data, Edge Function, or cryptographic-secret change.
  - End-to-end evidence: rerun attempt 2 of diagnostic run `30443924751`, job `90559844451`, traversed the production Vercel route with a synthetic nonexistent account and returned exact `401 invalid_staff_credentials`; no valid credential was supplied and no production mutation was performed by the diagnostic.
  - Regression prevention: the production web-session provisioning workflow and its repository contract now require `cf-connecting-ip`, preventing a later manual provisioning run from restoring the broken `x-real-ip` binding.
  - Unresolved blocker: none for the trusted-header boundary. The subsequent real-account authorization failure is owned separately by `BETA-PROD-ADMIN-LOGIN-005`.
  - Next exact roadmap item: `BETA-PROD-ADMIN-LOGIN-005`.

- **`BETA-PROD-ADMIN-LOGIN-005` — Production Admin login schema and metadata convergence**
  - Status: `IN_PROGRESS`
  - Current owner branch: `agent/production-admin-bootstrap-schema-trigger-cleanup-v1`; the schema repair was owned by `agent/production-admin-bootstrap-schema-reconcile-v1`, and the preceding Staff repair was owned by `agent/production-admin-staff-security-reconcile-v1`.
  - Production fault chain: a real administrator password reached Supabase Auth successfully, but the deployed login handler masked missing authorization and bootstrap projections as credential failures.
  - Staff root cause and repair: production `public.staff_users` had only the legacy six columns, and the linked Auth identity lacked the matching controlled `app_metadata`. PR #415 added the protected reconciliation plane; protected run `30450963053` then applied `20260726091000_add_staff_security_state_v2`, reconciled controlled Auth metadata, and proved all Staff schema, metadata, ledger, and PostgREST postconditions. PR #418 restored that workflow to manual-only after the one-time trigger.
  - Bootstrap root cause: after Staff authorization became healthy, the deployed handler's next projection required `public.game_sessions.game_join_code`, but production had only the other six bootstrap columns. Applying the older memorable-code migration wholesale was unsafe because it also defines game-creation functions whose prerequisites are absent in production.
  - Forward repair: PR #419 merged as `ebe62a64d6cd4234f7b2792020b519a6a15ff39e`. `backend/supabase/migrations/20260729123000_reconcile_admin_bootstrap_join_code_v1.sql` adds only the nullable `game_join_code` column, its format constraint, its partial unique index, and its comment. Protected run `30454172663` applied that exact migration and passed every authorization, precondition, atomic ledger, direct SQL, and PostgREST postcondition.
  - Authorization manifest: `docs/operations/evidence/production-admin-bootstrap-schema-reconciliation-v1.json`.
  - Safety boundary: exact merged-main SHA, exact production project binding, staging denial, production environment protection, aggregate-only evidence, no application-row writes or backfill, no database-function writes, no Auth metadata writes, no Edge deployment, no secret rotation, and no destructive rollback.
  - Runtime evidence: all 48 exact-head repository checks passed. Independent production SQL verification found all seven bootstrap columns, the nullable text column, validated constraint, valid unique index, zero invalid codes, one exact ledger row, one bootstrap projection row, all eight Staff security columns, one active Game Admin, zero missing Auth links, and zero metadata mismatches.
  - Trigger cleanup: `agent/production-admin-bootstrap-schema-trigger-cleanup-v1` restores `.github/workflows/production-admin-bootstrap-schema-reconcile.yml` to protected manual-only dispatch after the single successful push run.
  - Unresolved blocker: one successful real administrator login.
  - Next exact roadmap item: complete one real administrator login; if it succeeds, mark `BETA-PROD-ADMIN-LOGIN-005` `VERIFIED_COMPLETE`.

- **`BETA-BRAND-LOGIN-003` — Product-owner login logo replacement**
  - Status: `VERIFIED_COMPLETE`
  - Owner branch: `fix/login-logo-replacement-v2`
  - Pull request: #411, merged 2026-07-29.
  - Implementation commit: `fb452e5979e28c9ae35eedb0616ab6c68374ccda`.
  - Merge commit: `e13de3aff98e17730892d7e3550eaffe911b78e1`.
  - Dependencies: existing asset-backed login wiring merged by PR #400.
  - Beta impact: visual branding only; no feature semantics, authentication, API, database, or production-deployment change.
  - Implementation files: `assets/brand/Econovaria Logo.png`, `index.html`, `frontend/src/core/constants.js`, and `scripts/login-surface-browser-smoke.mjs`.
  - Migrations, routes, and RPCs: none.
  - Validation: exact supplied-file SHA-256 `f19b3ae263d2183b6b208d464996ec5f3f94bb6f2471b66b39557d19a8413726`; 2048×1149 TrueColorAlpha PNG with transparent pixels; `npm run audit:assets`; JavaScript syntax checks; HTTP `image/png` response and byte-identity check; cache-key consistency at `20260729.4`.
  - Staging/runtime evidence: all 12 required repository workflows and the Vercel preview passed before merge; production deployment was not part of this visual-only change.
  - Unresolved blocker: none.
  - Next exact roadmap item: `BETA-BRAND-LOGIN-004`.

- **`BETA-BRAND-LOGIN-004` — Enlarge the login logo treatment**
  - Status: `VERIFIED_COMPLETE`
  - Owner branch: `fix/login-logo-sizing-v1`
  - Pull request: #413, merged 2026-07-29.
  - Implementation commit: `35d91cbb80a4579fa01b2a758373295710a655d6`.
  - Merge commit: `7bc72758162bf5a9b955fb93c40fecf096da6674`.
  - Dependencies: `BETA-BRAND-LOGIN-003`, merged by PR #411.
  - Beta impact: visual branding only; no feature semantics, authentication, API, database, asset, or production-deployment change.
  - Implementation files: `index.html` and `scripts/login-surface-browser-smoke.mjs`.
  - Migrations, routes, and RPCs: none.
  - Validation: `npm run audit:assets`; JavaScript syntax check; whitespace validation; computed responsive crop confirms approximately 262×204 px of visible artwork inside a 312×212 px header window.
  - Staging/runtime evidence: merged after required repository and browser checks; production asset wiring remains unchanged.
  - Unresolved blocker: none.
  - Next exact roadmap item: `BETA-BRAND-LOGIN-005`.

- **`BETA-BRAND-LOGIN-005` — Reduce the enlarged login logo treatment**
  - Status: `IMPLEMENTED_NOT_MERGED`
  - Owner branch: `fix/login-logo-sizing-v2`
  - Pull request: not opened.
  - Implementation commit: `cf55c507dde7a016cc502d6992ad03a030dc24b2`.
  - Dependencies: `BETA-BRAND-LOGIN-004`, merged by PR #413.
  - Beta impact: visual branding only; no feature semantics, authentication, API, database, asset, favicon, or production-deployment change.
  - Implementation files: `index.html` and `scripts/login-surface-browser-smoke.mjs`.
  - Migrations, routes, and RPCs: none.
  - Validation: `npm run audit:assets`; JavaScript syntax check; whitespace validation; 400×224.41 px computed rendered image inside the unchanged 312×212 px header window, with approximately 210×163 px of visible artwork.
  - Staging/runtime evidence: pending CI browser validation; production is unchanged.
  - Unresolved blocker: none.
  - Next exact roadmap item: publish the branch, open its pull request, and require the browser and repository checks before merge.

- **`BETA-PROGRESSION-TEST-CLOCK-001` — Deterministic Progression integration-event fixture clock**
  - Status: `IN_PROGRESS`
  - Fetched-main reconciliation: owner `fix/player-commerce-transfer-disclosure-20260818`, PR #642; implementation `cbd3c4415c1b2a8170ca7f0783b0e69cbb6691a7`. The correction fixes `NOW` at `2026-07-21T05:00:00.000Z` for all seven trusted Progression integration-event calls and changes no runtime, schema, route, RPC, UI, economic, idempotency, deployment, or Business authority.
  - Collision boundary: PR #654's embedded copy of `backend/src/domains/progression/services/progressionIntegrationEventService.test.ts` is superseded and receives no implementation credit. C4 and later Business branches must reconcile the fetched-main test fixture rather than restore the stale donor copy.
  - Evidence and completion boundary: focused Progression 28/28, backend smoke, `typecheck:all`, root tests, architecture/legacy/migration audits, formatting, secret, and diff checks passed at the implementation commit. Exact-head checks and normal merge remain pending, so this item is not `VERIFIED_COMPLETE`.
  - Next exact roadmap item: preserve the correction during current-main reconciliation of its owning branch and later Business integration.

- **`BETA-PLAYER-ACCEPTANCE-SYNC-001` — Deterministic Player commerce and Business acceptance reconciliation**
  - Status: `IN_PROGRESS`
  - Fetched-main reconciliation: owner `fix/player-commerce-transfer-disclosure-20260818`, PR #642; implementation chain through `7cef343c1af875c11d1d47d986a1f89ac0f7ed9d`, `faaabcd92313b9a8db1f2c2c494dc85f0763d436`, `b8ea61925dd5325e880bfc870f126175db684447`, `eb3bbf5f8ce65a4893a6fdf237fefa1704b49d22`, and `49f3988afe2f76aa5e07986b3ed200cd7b80fb3b`.
  - Scope and collision boundary: deterministic disclosure/focus retries and Business mutation-completion synchronization are test-harness corrections only. PR #648's duplicate harness/manifest edits are superseded and must be de-duplicated during final integration; PR #624 retains its CSS scope. C4 and later Business branches must preserve the fetched-main versions of `scripts/business-banking-player-commerce-browser-acceptance.mjs`, `scripts/business-banking-player-business-browser-acceptance.mjs`, and `docs/operations/contracts/player-cross-cutting-verification-authority-v1.json` when those paths are reconciled.
  - Evidence and completion boundary: source transforms, authority tests, Player verification, focused Progression, backend smoke, `typecheck:all`, root tests, formatting, secret, and diff checks passed; connected browser evidence and exact-head PR checks remain pending. No runtime, schema, route, RPC, deployment, or live-data change is authorized.
  - Next exact roadmap item: preserve these acceptance-harness corrections in the final #648 current-main integration and rerun their connected Player gates.
