# REF-003 — Classify dead-code candidates with reproducible evidence

Status: VERIFIED_COMPLETE. Risk: R1. Dependencies: REF-002. Maps to ARCH-700.

## Read first

`scripts/architecture/build-architecture-inventory.mjs`, its current JSON inventory, the existing architecture ratchet, `scripts/legacy-runtime/`, `ops/legacy-runtime/` and REF-002 entrypoint/caller data. Read actual matching code before assigning a disposition.

## Allowed change

An evidence-backed candidate register and, only if needed, a small extension beside the existing inventory generator plus its focused tests. Proposed register: `docs/operations/evidence/refactor-execution-v1/REF-003/candidates.json`. No application deletion, baseline inflation or migration edits. Budget: eight source/test/evidence files.

## Implementation

1. Recompute counts at the exact source SHA. Separate reachable production source, tests, fixtures, generated output, scripts, historical archives and migration history. Report both file counts and physical-line denominators; do not call either executable-code coverage.
2. Assign one primary disposition per candidate: canonical, confirmed_dead, compatibility_required, legacy_candidate, historical_archive, defensive_fallback, generated_or_fixture or unknown. Track overlapping structural flags separately so percentages are not added twice.
3. Record path/symbol, owner, reason, all inbound roots, dynamic/HTML/build references, SQL/job/HTTP consumers, replacement, evidence query, confidence and removal conditions. `safeToDelete` defaults false.
4. Treat fallback images/defaults, accessibility observers, absent nullable fields and intentional negative-state tests as normal behavior unless source evidence says otherwise. `v1`, `legacy`, file age or zero direct imports is insufficient.
5. Select no more than three low-risk deletion candidates for REF-015/047, each with a proof checklist. External traffic evidence must follow the existing retention policy; do not invent a zero-traffic result.

## Acceptance and validation

The register is reproducible, resolves paths, distinguishes static proof from runtime proof and includes unknowns. Scanner tests include a real defensive fallback, dynamically loaded module, test-only match and externally dispatched handler. Existing architecture ceilings do not increase. Run the shared architecture/legacy checks and JSON/diff checks.

## Stop / rollback

No percentage deletion target is allowed. Retain ambiguous candidates. Revert only the classifier extension if it misclassifies a fixture; application and migration history remain untouched.

## Implementation checkpoint

Existing owner: draft PR #740, `refactor/ref-003-dead-code-candidates`, from main `a64befd325a3b46fdc86b9bcd3dae60641545697`. This continuation implements the classifier and initial reviewed register rather than merely updating a status. Read [scope](../../../operations/evidence/refactor-execution-v1/REF-003/scope.md), [candidate register](../../../operations/evidence/refactor-execution-v1/REF-003/candidates.json) and [validation evidence](../../../operations/evidence/refactor-execution-v1/REF-003/validation.json) together.

Tooling checkpoint: `440ea2cb48cd37f59b492d3331f623ab7b9f2e34`. `scripts/architecture/refactor-candidate-audit.mjs` reads immutable Git objects for an exact revision, reuses the existing architecture candidate inventory, separates physical-file categories and emits conservative dispositions plus source-reference evidence. It reads no production credentials, follows no working-tree symlinks, changes no tracked source and never infers deletion eligibility from lexical markers. Nested tooling and ambiguous generic basenames have dedicated regression cases.

The full-checkout CI census at that checkpoint covered 3,951 tracked files and 738 static candidates: 2 canonical, 3 compatibility-required, 1 defensive fallback, 175 generated/test/fixture, and 557 unknown. Eight entries have hash-bound source reviews; these eight overlap the disposition totals. No candidate is confirmed dead or approved for deletion. Application-source category counts are not proven runtime reachability. Full generated rows are reproducible with `node scripts/architecture/refactor-candidate-audit.mjs 440ea2cb48cd37f59b492d3331f623ab7b9f2e34`; the committed seed register is not a claim that all 738 files were semantically reviewed.

Existing retirement CI run `35798323328`, job `106982684240`, passed all 17 tests (10 classifier fixtures, 1 real-source census, 6 retained retirement tests) on Node 22.23.1/npm 10.9.8. Its actual checkout was synthetic PR test merge `46c98747c03da66e5bc4fc2dc52156f1b8254428`, whose tree `e21f74f722f8a544ccb1a359a1f171278ea8b36d` matches the tooling checkpoint. It is not an actual PR merge. Repository Quality run `35798323261` passed the existing npm-test/audit and credential-scan steps. New metadata commits require their own final-head checks; earlier results are not relabeled.

The task remains IN_PROGRESS. Remaining work is semantic review/ownership and full caller/replacement evidence for unresolved candidates, followed by acceptance review and an authorized normal merge. Unknown external use remains a retention gate, not evidence of dead code. Existing architecture output/ceilings, other task objects/dependencies, application source, migrations, workflows and production settings remain protected. REF-004 and later tasks have not started.

## Source-review continuation — 2026-09-23

Source and review checkpoint: `5a6bbf7c54d16a2c77d2fdd3043b419b9fd05874`. The register now contains 17 reviewed records, preserving the original eight and adding Attendance request composition, Player identity/creation responsibilities, historical Apps Script provenance, canonical BFF path validation and three distinct Deno configuration roles. Fourteen supporting-source hash bindings are enforced; older path-only supporting references remain explicitly labeled. No current compiler configuration was changed.

The historical-path coverage gap is closed: all nine such files enter the candidate register, even when small or binary. Their path category does not grant an archival disposition or deletion approval. Every generated candidate now exposes query, symbol-review state, confidence, consumer-audit and replacement fields, using NOT_REVIEWED or NOT_SELECTED where evidence is missing rather than silently suggesting clearance.

Pinned full-checkout CI run `35800007942`, job `106987970909`, passed 21/21 tests (14 fixtures, one real-source census and six unchanged retirement tests), plus legacy policy/transport and Player cutover checks. Its synthetic checkout `a4dc419e11e92a82f0f79026e612e7d1256e0eb6` has tree `604e3f9b3a59cee3660fd6ba70fc0cd542edc25a`, identical to the review checkpoint. Repository Quality run `35800007851`, job `106987970962`, passed npm test and committed-source scanning. These are executed tooling/source checks, not an actual merge or authenticated runtime evidence.

The measured snapshot contains 3,952 tracked files and 745 candidates: 5 canonical, 7 compatibility-required, 2 historical archives, 1 defensive fallback, 175 generated/test/fixture and 555 unknown. The seven added candidates reflect broader measurement, not new application code. Application-source, generated-output and migration-history file/line denominators are unchanged from the prior checkpoint. No candidate is confirmed dead; the deletion shortlist remains empty.

Remaining REF-003 review is not converted into completion by these passing tests. The next bounded source families include the historical API router and stock-history helpers, plus the remaining explicit/dynamic callers of retained Player identity and Attendance save code. The full regenerated unknown register remains authoritative for further triage; this paragraph is not a new backlog or permission to delete. Preserve source ownership and record unresolved external use as a retention gate. REF-004 is unstarted.

## Archive and event-consumer checkpoint — 2026-09-23

Tooling/source-review head: `cd5c85c5def487a141720273f63b0a78dd27e284`, inspected from the preceding published `32b35fb857e67d5107b8e83965f1153a2bdfc689`. The two archived request/history families and four retained browser controllers named in the prior checkpoint now have source reviews: 23 reviewed records total and 32 supporting-source hash bindings. All prior reviews remain. Event/string-dispatch terms are recorded separately from symbols, must occur in the reviewed blob, and produce labeled literal-reference evidence rather than invented event execution or transitive calls.

The actual CI checkout was synthetic PR merge `123ebb1bb86d4295d84167f0cc1f6cc389b599c6`, tree `8e5d42f646b01032b558024edaac26a080f2ac6b`, independently matched to the tooling head. Retirement run `35801868541`, job `106993811580`, passed 24/24 tests on Node 22.23.1/npm 10.9.8: 17 classifier fixtures, one real-source census and six original retirement tests. It also passed legacy policy/transport and static Player cutover checks. Repository Quality run `35801868512`, job `106993810002`, passed the existing npm-test/audit and credential-scan steps. These results belong to this immutable checkpoint, not later documentation commits.

Measured source: 3,952 tracked files, 746 static candidates, seven reviews with event/dispatch terms, and all nine historical-path files included. Primary dispositions: 5 canonical, 11 compatibility-required, 4 historical archives, 1 defensive fallback, 175 generated/test/fixture and 550 unknown. Reviewed counts overlap these totals. Application source remains 997 files / 242,943 physical lines; generated output remains 2 / 14,046; migration history remains 450 / 161,859. No file is confirmed dead, no deletion is approved, and no runtime reachability percentage is claimed.

The new reviews preserve important limits: archived sheet writes are sequential rather than modern atomic transactions; archived history setup and maintenance can install jobs or delete rows; Player profile saving PATCHes profile then updates credentials without a full post-await modal-generation guard; Attendance generation checks suppress stale UI but do not abort requests; Settings mounted readiness is a UI signal, not proof of a successful fresh read. These observations do not authorize behavior fixes.

Follow-up source disambiguation is recorded in validation.json: Player creation UX consumes the issued-credential event separately from modal closure; simplified Settings emits context changes and acknowledges save events; an archived `logStockPriceHistory_` match is a different underscore-suffixed function, not a proven caller of `logStockPriceHistory`. Generic local symbols such as `reconcile` and `loadPlayers` also yield lexical collisions; literal hits are not credited as semantic caller edges.

REF-003 remains IN_PROGRESS for bounded acceptance review and unresolved source evidence. The new named reviews close the previous checkpoint's specific source gaps; they do not claim every unknown candidate was semantically inspected. Unknown hosted use remains a retention gate, not a requirement to fabricate zero traffic or a deletion quota. Preserve all eight-file boundaries and require actual final-head CI/diff review before any merge/completion claim. No application, migration, workflow, package, database, production or REF-004 change occurred.

## Bounded task acceptance — 2026-09-23

Acceptance decision: the source-classification and tooling requirements are satisfied at `52953c06293d4935490d1a71fd6e45b56b74a99b`; publication remains IN_PROGRESS until this record's final-head checks pass and a normal merge actually occurs. No prospective merge identity is recorded.

| Requirement | Evidence and disposition |
| --- | --- |
| Exact tracked-source census | Immutable Git tree census covers 3,952 tracked files, with mutually exclusive file/physical-line denominators. Physical application-source categories are not falsely described as proven production reachability. Unknown reachability is explicitly retained. |
| Primary dispositions and structural overlap | 746 candidates have one primary disposition each: 5 canonical, 11 compatibility-required, 4 historical archives, 1 defensive fallback, 175 generated/test/fixture, 550 unknown, zero confirmed-dead and zero legacy-candidate nominations. Overlapping flags and the 23 reviewed records are not added to those totals. |
| Required candidate fields and source proof | Generated rows expose path/hash, symbol/review state, owner, reason, source/consumer evidence, replacement state, exact query, confidence and removal conditions. The 23 reviewed records and 32 supporting-source bindings preserve positive caller evidence. Unknown fields are explicit NOT_REVIEWED/NOT_SELECTED states, not false clearance. |
| Defensive and non-import callers | Real AdminMedia, dynamic bootstrap, external Stocks handler and separate test-only/event fixtures pass. Event and generic-symbol collisions remain literal discovery evidence, not fabricated calls. |
| Removal shortlist | Empty. At most three is a ceiling, not a quota. No code is nominated for REF-015/047 deletion without its required evidence. All safeToDelete values remain false. |
| Original architecture authority | Existing generator, inventory and ceilings are unchanged. The root npm-test audit chain independently regenerates the original inventory and checks a zero diff; the candidate classifier correctly reports that it does not itself regenerate that authority. |
| Evidence consistency | The real-source test parses saved JSON, checks category/disposition sums and latest-measurement pointers, verifies source bindings and resolves task companion links. Historical snapshots are not overwritten with newer source identities. |
| Entire observed PR diff and protected metadata | The new exact-blob eight-file fixture passes git diff --check, including intent-to-add files. The original task requirement text, original retirement suite and original backlog excluding only REF-003's status/identity fields reproduce the three independently read main blob hashes. No other task object or dependency can be silently rewritten. |

Retirement CI run `35805410699`, job `107004997262`, executed the new diff check and passed all 24 tests on Node 22.23.1/npm 10.9.8. At 2026-09-23 01:13:48 UTC, `REF003_DIFF_CHECK` recorded PASS for the complete observed eight-file changed-path fixture. This is not a full local application checkout. Its actual source was synthetic PR merge `700c382659d2633ddcb0a262854958a269dddfca`, tree `c6b6311d3c0cd23cdf297187fb2754b77e676cb4`; the Git-data endpoint independently confirms the branch checkpoint has the same tree. The same job passed legacy inventory/transport and static Player cutover checks. The census record digest is `ff6ebfcffeb9086c443aa0c90aab04306a2b008ec119479ac3426bc5425d6b13`; reproduce that digest with the exact executed SHA because queries include it.

This acceptance does not turn the 550 unknown records into reviewed code. Missing semantic ownership, complete transitive/browser/SQL/job caller analysis, replacement implementation, and hosted-use evidence remain per-candidate retention gates before any later removal. The previous named review families are closed as source classifications, not runtime or transaction-parity certification. Earlier checkpoint language requiring every unknown to receive complete semantic review before this bounded register can close is superseded in that respect only, consistent with this task's original explicit allowance for unknowns.

Unexecuted and uncredited: a complete local application checkout; standalone backend typecheck:all/smoke; authenticated staging/production browser/API probes; live SQL, scheduler, external-client or traffic observation. This R1 classification tool changes none of those application surfaces. Later runtime refactors still require their applicable shared tests and retirement windows. The existing 14-day pre-disable, 7-day monitoring and 30-day pre-delete recovery policy is unchanged, and no quiet window is claimed.

The candidate/validation JSON files preserve their historical execution snapshots and statuses; this task record and its backlog object carry subsequent acceptance/publication status. Final-head workflow results and the actual merge identity must be recorded only after they exist. No application/browser source, bundle, migration, runtime manifest, package, workflow, cloud setting or production release changed. REF-004 remains PLANNED and unstarted.

## Actual merge and verification closeout — 2026-09-23

Current task status: **VERIFIED_COMPLETE**, for the bounded classifier and conservative register only. The checkpoint paragraphs above retain their historical states. PR **#740** was normally merged at **2026-09-23 01:31:50 UTC**, after all eight final-head workflows and the Vercel commit status passed. GitHub's actual merge commit is **`c15d30ebe697c0cd2ca1d34c59c8e4366cb988dc`**; final implementation head is **`b0fc0788c3ce06fd470cbd66df1fb1e1918ed048`**. Both have tree **`1425bfb14298fa3c6faf51ecf43f2d2b18aac263`**. This is actual integration, not a synthetic PR test merge.

| Final-head workflow | Run | Result |
| --- | --- | --- |
| Repository Quality | 35805754625 | SUCCESS |
| Legacy Runtime Retirement Audit | 35805754604 | SUCCESS |
| Supply Chain Security | 35805754598 | SUCCESS |
| Release Integrity | 35805754654 | SUCCESS |
| Production Runtime Promotion Contract | 35805754492 | SUCCESS |
| Release Promote Exact Artifacts | 35805754655 | SUCCESS |
| Staging Readiness Preflight | 35805754537 | SUCCESS |
| Business Player Store Cutover V2 | 35805754502 | SUCCESS |

The final Business workflow also completed its full Chromium job `107006302263`, two-game connected Store journey `107006302122`, twice-replayed disposable database/lint job `107006302267`, serial/concurrent settlement and isolation job `107006302389`, backend TypeScript/all-Edge-roots job `107006302249`, and original authority/inventory job `107006302282`. These are actual disposable CI results, not production traffic or live-database acceptance. A standalone backend smoke command remains uncredited.

This closeout changes only this task record and REF-003's existing backlog fields: status, implementationSha, mergeSha and evidence. The serialized backlog was checked against independently fetched exact baseline/current Git blobs; all other 49 task objects and all dependency links remain unchanged. The two-file closeout itself proceeds through a normal reviewed PR. No prospective closeout-merge SHA is embedded, and the implementation merge identity above remains fixed.

All 550 unknown classifications and all other retained candidates still prohibit deletion without the individual missing evidence; no semantic-review completion is assigned to unknown records. The frozen candidate/validation JSON reports remain historical execution evidence, not the current task-status ledger. No application behavior, migration, runtime configuration, workflow, dependency, secret or live cloud/database setting was changed, and no production deployment or release was dispatched by this task. **REF-004 and later tasks remain unstarted.**
