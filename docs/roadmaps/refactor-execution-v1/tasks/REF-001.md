# REF-001 — Freeze the exact-main baseline and resolve owner gates

Status: **VERIFIED_COMPLETE — documentation-only baseline**. Risk: R0. Dependencies: none. Maps to ARCH-000 refresh, not reopening its completed implementation. This is not production or application certification.

## Read first

Root AGENTS.md and CONTRIBUTING.md; beta completion ledger; architecture-hardening roadmap; current checkpoint and Phase 15 evidence referenced by them; root/backend/Player package manifests; current main/PR/check metadata. Recheck #668, #730, #731, #735 and #736, not just their planning snapshots.

## Allowed change

Only `docs/operations/evidence/refactor-execution-v1/REF-001/baseline.json`, `baseline.md` and this package's task/intake/status metadata. The global beta ledger may be updated only by its resolved controller owner. No runtime/source/configuration edits. Budget: five documentation files. The owner-approved scoped acknowledgement is in [SCOPE-INTAKE.md](../SCOPE-INTAKE.md); no global ledger edit or context-implementation takeover is included.

## Implementation

1. Record full main SHA, tree SHA, branch cleanliness and collection time. Record exact source identity for every check; separate main, PR head, staging and production identities.
2. Resolve active ownership, overlapping files and unresolved incidents. Specifically distinguish Player-login source repair, canonical-origin release work, Phase 15 certification and ARCH-100F context work. Do not choose between competing release owners by silently superseding one.
3. Inventory available baseline results: architecture/legacy audits, typechecks, focused auth/contract suites and authenticated runtime evidence. Run only locally safe read/test commands in an authorized environment. Missing access/tools or stale results stay NOT_RUN/RE_AUDIT_REQUIRED.
4. Save each known failing flow with reproduction class and owning PR, without credentials or student data. A healthy public endpoint does not close login failures.
5. Record the scoped intake/controller acknowledgement and which next ticket may run. Documentation-only measurement may proceed despite a production incident; auth/runtime refactoring in the affected seam cannot.

## Acceptance and validation

All baseline fields identify an exact source or explicitly say unknown. Every open blocker has an owner/next action. No task is given completion credit from old scores, PR prose or unmerged work. Validate JSON, links and `git diff --check`; use the shared matrix only where execution is safe and report what was actually run.

## Stop / rollback

Stop affected code activation if current ownership or baseline behavior cannot be established. Roll back only this bounded documentation record; never change deployment, secrets, migrations or other owner branches to manufacture a green baseline.

## Historical checkpoint — 2026-09-22, 04:41:09 UTC

The [original immutable baseline](https://github.com/kohnerbouchard-star/Student-Profile/blob/daa6acc4fb9bda82d1e04d091ed4719d16c583e8/docs/operations/evidence/refactor-execution-v1/REF-001/baseline.md) captured the source, selected CI, ownership conflicts, known incidents and unavailable execution. It initially classified the task BLOCKED. That classification was too broad for a documentation-only baseline. The original checkpoint remains preserved; it is not a statement of current task status.

## Accepted closeout — 2026-09-22

The product owner approved the explained bounded closeout with “Ok then let’s do it and continue the tasks.” The [scoped intake](../SCOPE-INTAKE.md) records direct owner acknowledgement for REF documentation. It does not assert a reply from #668, reassign its context implementation, merge another owner branch or overwrite the global completion ledger. Phase 15/authentication/tooling limitations remain activation gates for affected later source changes, not blockers to publishing an honest baseline.

Audited application source remains `196b88e6dec9fc951ae0e690fe96057d67bf7bf5`, tree `ac2fd0125af42c9015f9eae325dccc1eff6cabc3`. Accepted evidence implementation is `554a67cc54b9bc57c4e8464382e9c278c5a46fb1`. [PR #738](https://github.com/kohnerbouchard-star/Student-Profile/pull/738) merged normally at **`346aa8235e57d31cafa6d84b9a6bb0dd33933dbb`**, 2026-09-22T06:40:55Z. Comparing the accepted head to this merge returns no changed files: the checked evidence and merge trees are identical. The audited source is not rewritten to the documentation merge.

Accepted evidence: [baseline.md](../../../operations/evidence/refactor-execution-v1/REF-001/baseline.md) and [baseline.json](../../../operations/evidence/refactor-execution-v1/REF-001/baseline.json). The JSON's `taskStatusAtRevision` is its historical pre-merge state; current status and actual identities are recorded in [backlog.json](../backlog.json). The completion entry is published through PR #737 and does not invent that PR's future merge SHA.

### Verification supporting this documentation closeout

At exact evidence head `554a67cc54b9bc57c4e8464382e9c278c5a46fb1`, the returned PR workflows passed: Repository Quality `35695551087`; Staging Readiness Preflight `35695550963`; Release Integrity `35695550976`; Production Runtime Promotion Contract `35695551022`; Supply Chain Security `35695551019`; Release Promote Exact Artifacts `35695550975`. Vercel status passed. There were no review submissions or inline review threads when checked; no independent third-party review is claimed.

At actual merge `346aa8235e57d31cafa6d84b9a6bb0dd33933dbb`, Repository Quality `35695941735`, Supply Chain Security `35695941823` and Beta Security Contract `35695941741` passed. Production Git Release `35695941808` passed its static contract only; staging capture, production capture, live-parity enforcement and release-branch publication were skipped. No production dispatch was made.

Local documents-only JSON, source-identity, scope, companion-link and whitespace checks passed. The revised evidence blobs exactly match the locally checked bytes: JSON `269f7aeb4bd33bbf6cbfa1726cf5262ff88fd0d2`; Markdown `80ccebe06437e58fbc22fdf9192c20cf6ea6a31a`. All 50 task IDs and 123 dependency edges are retained, with only REF-001 receiving completion credit. The other 49 task objects and all prerequisite edges remain unchanged.

### Limitations deliberately retained

Local application checkout, pinned application tests, backend typechecks, connected browser tests, fresh provider inspection and authenticated runtime probes remain **NOT_RUN** in this execution environment. Existing/public-health CI is not authenticated session evidence. Reported login incidents, unknown Edge/migration/staging identities, Phase 15 coordination and shared-file ownership remain explicitly protected. No gameplay, runtime, schema, workflow, release request or cloud setting changed.

**Stop after REF-001. REF-002 remains PLANNED and has not started.** The next owner may perform read-only route/import mapping after this documentation closeout is on main, rechecking current source and ownership first. Later code tasks still require every applicable behavior-parity and execution gate.
