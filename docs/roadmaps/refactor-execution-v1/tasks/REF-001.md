# REF-001 — Freeze the exact-main baseline and resolve owner gates

Status: BLOCKED — baseline captured; not VERIFIED_COMPLETE. Risk: R0. Dependencies: none. Maps to ARCH-000 refresh, not reopening its completed implementation.

## Read first

Root AGENTS.md and CONTRIBUTING.md; beta completion ledger; architecture-hardening roadmap; current checkpoint and Phase 15 evidence referenced by them; root/backend/Player package manifests; current main/PR/check metadata. Recheck #668, #730, #731, #735 and #736, not just their planning snapshots.

## Allowed change

Only proposed `docs/operations/evidence/refactor-execution-v1/REF-001/baseline.json`, `baseline.md` and this package's task-status metadata. The global beta ledger may be updated only by its resolved controller owner. No runtime/source/configuration edits. Budget: five documentation files.

## Implementation

1. Record full main SHA, tree SHA, branch cleanliness and collection time. Record exact source identity for every check; separate main, PR head, staging and production identities.
2. Resolve active ownership, overlapping files and unresolved incidents. Specifically distinguish Player-login source repair, canonical-origin release work, Phase 15 certification and ARCH-100F context work. Do not choose between competing release owners by silently superseding one.
3. Inventory available baseline results: architecture/legacy audits, typechecks, focused auth/contract suites and authenticated runtime evidence. Run only locally safe read/test commands in an authorized environment. Missing access/tools or stale results stay NOT_RUN/RE_AUDIT_REQUIRED.
4. Save each known failing flow with reproduction class and owning PR, without credentials or student data. A healthy public endpoint does not close login failures.
5. Record the scoped intake/controller acknowledgement and which next ticket may run. Documentation-only measurement may proceed despite a production incident; auth/runtime refactoring in the affected seam cannot.

## Acceptance and validation

All baseline fields identify an exact source or explicitly say unknown. Every open blocker has an owner/next action. No task is given completion credit from old scores, PR prose or unmerged work. Validate JSON, links and `git diff --check`; use the shared matrix only where execution is safe and report what was actually run.

## Stop / rollback

Stop affected code activation if current ownership or baseline behavior cannot be established. Roll back only this new documentation record; never change deployment, secrets, migrations or other owner branches to manufacture a green baseline.

## Execution checkpoint — 2026-09-22

[Baseline evidence PR #738](https://github.com/kohnerbouchard-star/Student-Profile/pull/738) adds only `baseline.json` and `baseline.md`, from main `196b88e6dec9fc951ae0e690fe96057d67bf7bf5`. Evidence commit: `daa6acc4fb9bda82d1e04d091ed4719d16c583e8`. Read the [immutable baseline](https://github.com/kohnerbouchard-star/Student-Profile/blob/daa6acc4fb9bda82d1e04d091ed4719d16c583e8/docs/operations/evidence/refactor-execution-v1/REF-001/baseline.md).

Observed main/release Git identity, selected existing CI, active owner heads and changed-file collisions are recorded. Controller acknowledgement associated with #668 is not established; Phase 15 ownership (#730/#731) and fresh authenticated runtime evidence (#735/#736) remain unresolved. No affected source refactor is activated. Existing CI is not a fresh local test run.

JSON/link/scope validation and document-only `git diff --check` passed. The application clone failed on DNS resolution; required local Node/npm pins, Deno and Docker were unavailable, so application tests/typechecks and authenticated probes are NOT_RUN. Remote evidence blobs match the validated local files. Planning and evidence PRs remain unmerged; the global completion ledger is unchanged.

**Stop after REF-001. REF-002 is not started.** Resume with controller intake and evidence review; do not bypass the separate runtime gates or create a duplicate context/release owner.
