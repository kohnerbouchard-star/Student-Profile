# REF-048 — Prepare gated Classroom compatibility-source retirement

Status: PLANNED. Risk: R3. Dependencies: REF-006, REF-007, REF-008, REF-009, REF-010, REF-011, REF-015. Maps to ARCH-700. Reconcile #730/#731/#735/#736 and current release ownership.

## Read first

Classroom README/source/configuration, remaining `proxyClassroom` callers and tests, all Edge imports, deployment/release manifests, `ops/legacy-runtime/`, retirement policy and the refreshed REF-002/003 caller register. Prior removal of known routes is not proof of zero remaining usage.

## Allowed change

First a retirement dossier and exact residual-reference map. A source/config deletion candidate is permitted only as a separately bounded child after all required gates and explicit owner approval; no cloud disablement/deletion or production deployment in this ticket. Split source/config/test changes into reviewed children if more than 15 meaningful files.

## Implementation

1. Prove zero required canonical browser/BFF/API forwarding, cross-root implementation import, unrelated Deno-config dependency and unresolved route alias. Classify historical documentation separately.
2. Identify external HTTP/job/monitor consumers and collect authorized traffic evidence under the current retirement policy. Unknown or successful required legacy traffic blocks removal; do not shorten the observation window in this package.
3. Preserve the last deployed source/configuration identity and rollback owner. Verify that replacement APIs retain all auth, scope, replay, rate-limit and economic guarantees.
4. Prepare the exact candidate removing only approved compatibility source and obsolete registration/release references. Keep equivalent canonical behavioral tests and retirement ratchets; do not delete test requirements because their former path vanished.
5. Obtain separately authorized staging/release execution and zero-use evidence through the existing controller. Record source-prepared, staging-verified and production-retired as distinct states.

## Acceptance and validation

The dossier proves every removal gate or explicitly lists what remains BLOCKED. Source retirement is not complete while any required caller/config/runtime consumer remains. Run full auth/Admin/Player/domain suites, typechecks, legacy/asset/release inventories and shared checks; connected staging and production evidence are separate requirements, not simulated results.

## Stop / rollback

No automatic runtime deletion. Rollback restores only the reviewed retained source/configuration through an authorized release; it never restores revoked secrets or bypasses current security. This parent cannot claim runtime retirement from a docs PR or an unused-import scan.
