# REF-002 — Map canonical routes, import roots and browser entrypoints

Status: IN_PROGRESS. Risk: R0. Dependencies: REF-001. Maps to ARCH-400/ARCH-700.

## Read first

`backend/supabase/functions/admin-api/gameRoutes.ts`, `common.ts`, `compatibilityOperations.ts`; `backend/supabase/functions/player-api/runtime.ts`; `backend/supabase/functions/classroom-api/index.ts` and README; current BFF source, HTML entrypoints, runtime configuration and deployment manifests discovered through `git ls-files`. Read both modern and retained Admin/Player loading paths.

## Allowed change

Proposed evidence files `routes.json`, `entrypoints.json`, `caller-map.md` under REF-002 only. Reuse existing inventories; do not create a competing runtime registry. No route, import, deployment or source edits. Budget: six documentation/data files.

## Implementation

1. Trace each public method/path through browser caller, BFF, auth guard, Edge dispatcher, application handler, repository and SQL/RPC. Separate canonical routes, aliases, compatibility-only routes and externally callable workers.
2. For every Classroom dependency classify HTTP forwarding, source import, test configuration, browser alias, deployment requirement or historical mention. A documentation reference is not a live caller; an externally callable endpoint is not dead because imports are zero.
3. Record the precise residual routes visible in `gameRoutes.ts`: progress GET; decision/submissions-review/progress-review aliases; reward issue; access-code reset. Verify any additional proxy calls rather than assuming this is the exhaustive list.
4. Trace HTML script tags, generated-bundle inputs, dynamically constructed imports, fetch interception, DOM observers and event listeners. Record mount/dispose ownership and which browser surface loads each shim.
5. Map each route's actor/game resolution, permission, identifier translation, rate-limit and idempotency enforcement point. Record current response/header boundaries and external consumers as unknown unless evidenced.

## Acceptance and validation

Every candidate selected for REF-006–015 has a complete inbound/outbound path and named canonical target. Unknown entrypoints are explicit blockers. Compare inventory paths against `git ls-files`; validate method/path uniqueness without collapsing distinct aliases. Record query commands and source SHA.

## Stop / rollback

No live disablement or deletion is permitted. Missing dynamic/external reachability evidence remains unknown. Revert the evidence commit if malformed; do not remove routes to make the census simpler.

## Execution checkpoint — 2026-09-22

Source main: `7569a829905ec5104e5b98c3e29e8dd24b158c0f`. Owner branch: `docs/ref-002-route-entrypoint-map-v1`; draft PR #739. The original published checkpoint is `fcf0489ce212207659dbe98061e58fcb113f8f62`.

Read the [current source report](../../../operations/evidence/refactor-execution-v1/REF-002/caller-map.md) first. It supersedes the earlier unreadable-bundle/build interpretation. The [23 detailed method/path cases](../../../operations/evidence/refactor-execution-v1/REF-002/routes.json) and [57-reference snapshot](../../../operations/evidence/refactor-execution-v1/REF-002/entrypoints.json) remain unchanged historical inputs, not a full application scan or deletion count.

The continuation reads the exact retained bundle through its Git blob and records all 79 action descriptors and 24 read descriptors, plus the Player catalog's 96 advertised method/path operations across 86 keys. There are 199 declarations and 194 distinct pre-translation pairs across the two namespaces. Duplicated caller names/pairs, browser rewrites, rejected methods and dynamic paths are not silently collapsed into working routes.

The build copies legacy assets but overwrites output `admin/index.html` with V2 HTML. Raw retained-page loading is distinct from the built default page. The authenticated legacy mount, selected Settings/Contract rewrites, dynamic export-status template and advertised-but-rejected threadless Messaging read are now documented. Existing atomic reward, credential-event, scope and Deno-strictness constraints are preserved.

MAP-01's bundle-access obstacle is resolved; complete wrapper/event-consumer reconciliation remains open. MAP-02 still requires actual Admin V2 and Player dispatch reconciliation, complete current BFF/worker/tracked-root coverage and dynamic-path resolution. MAP-03 requires final review/checks and actual merge after the original task requirements are satisfied. No requirement above has been weakened and no successor receives credit.

Local documentation checks cover table counts, keys/methods/path syntax, duplicate-pair preservation, source-identity formats, links and whitespace. The new report and task record are the only changed paths in this continuation; both JSON evidence files and the entire backlog manifest are preserved. No source, configuration, workflow, database or runtime changes. Complete checkout, application build, bundle-audit execution, backend/Deno, browser crawl and authenticated/cloud/SQL probes remain NOT_RUN.

**Stop at REF-002. REF-003 has not started.** Keep this task IN_PROGRESS until the remaining evidence and normal acceptance are complete; do not relabel a published partial map VERIFIED_COMPLETE.

### Continuation preflight

Live GitHub reads reconfirmed main `7569a829905ec5104e5b98c3e29e8dd24b158c0f` and the existing draft PR head `f92f1075d87468d81069753428e693044b80d82d`, with the same five documentation/data paths. Direct Git checkout failed because the execution environment could not resolve GitHub; archive transfer was unavailable. Connector source reads remain usable. No complete application checkout or source-audit execution is claimed by this preflight; all original mapping and acceptance gaps above remain open. The prior `FINISH-REF-002.md` handoff was located and is evidence to verify against source, not a replacement authority.
