# REF-002 — Map canonical routes, import roots and browser entrypoints

Status: IN_PROGRESS. Risk: R0. Dependencies: REF-001. Maps to ARCH-400/ARCH-700.

## Read first

`backend/supabase/functions/admin-api/gameRoutes.ts`, `common.ts`, `compatibilityOperations.ts`; `backend/supabase/functions/player-api/runtime.ts`; `backend/supabase/functions/classroom-api/index.ts` and README; current BFF source, HTML entrypoints, runtime configuration and deployment manifests discovered through `git ls-files`. Read both modern and retained Admin/Player loading paths.

## Allowed change

Proposed evidence files `routes.json`, `entrypoints.json`, `caller-map.md` under REF-002 only. Reuse existing inventories; do not create a competing runtime registry. No route, import, deployment or source edits. Budget: six documentation/data files.

## Implementation

1. Trace each public method/path through browser caller, BFF, auth guard, Edge dispatcher, application handler, repository and SQL/RPC. Separate canonical routes, aliases, compatibility-only routes and externally callable workers.
2. For every Classroom dependency classify HTTP forwarding, source import, test configuration, browser alias, deployment requirement or historical mention. A documentation reference is not a live caller; an externally callable endpoint is not dead because imports are zero.
3. Record the precise residual routes visible in `gameRoutes.ts`: progress GET; decision/submissions-review/progress-review aliases; reward issue; access-code reset. Verify any additional proxy calls rather than assuming this is the exhaustive list.
4. Trace HTML script tags, generated-bundle inputs, dynamically constructed imports, fetch interception, DOM observers and event listeners. Record mount/dispose ownership and which browser surface loads each shim.
5. Map each route's actor/game resolution, permission, identifier translation, rate-limit and idempotency enforcement point. Record current response/header boundaries and external consumers as unknown unless evidenced.

## Acceptance and validation

Every candidate selected for REF-006–015 has a complete inbound/outbound path and named canonical target. Unknown entrypoints are explicit blockers. Compare inventory paths against `git ls-files`; validate method/path uniqueness without collapsing distinct aliases. Record query commands and source SHA.

## Stop / rollback

No live disablement or deletion is permitted. Missing dynamic/external reachability evidence remains unknown. Revert the evidence commit if malformed; do not remove routes to make the census simpler.

## Execution checkpoint — 2026-09-22

Status remains **IN_PROGRESS**, not VERIFIED_COMPLETE. Existing owner branch: `docs/ref-002-route-entrypoint-map-v1`; draft PR #739. Audited main: `7569a829905ec5104e5b98c3e29e8dd24b158c0f`; source revision: `f92f1075d87468d81069753428e693044b80d82d`, tree `1aa13faf5016c78df27edc2ec0e90e0330364c99`. The publication preflight commit is `b13f7607e76844b9ef7aa6be2958b93eba437a33`. No actual merge has occurred at this checkpoint.

Read [caller-map.md](../../../operations/evidence/refactor-execution-v1/REF-002/caller-map.md), [routes.json](../../../operations/evidence/refactor-execution-v1/REF-002/routes.json) and [entrypoints.json](../../../operations/evidence/refactor-execution-v1/REF-002/entrypoints.json) together. The original 23 detailed cases, 57 source-reference records, 79 Admin action declarations, 24 Admin read declarations and 96 Player advertised operations across 86 keys are preserved. The declarations overlap and are not a working-endpoint or deletion total.

The evidence is reconciled to 24 detailed cases, eight separately classified export branches and 76 source-reference records. The dynamic GET export-status template is documented with direct-download short-circuiting, normal security/ownership failures and the Admin 501 unimplemented fallback. The six gameRoutes proxy-helper callsites are reconciled without mistaking locally intercepted atomic reward issuance for Classroom HTTP. All 16 tracked API file rows and all 29 Player outer-dispatch families are recorded; canonical Business still precedes the mixed facade. Filename/outer-dispatch coverage is not represented as complete forwarding-body or leaf-method coverage.

Both JSON files now agree with the report about readable immutable bundle sections, source declaration counts, raw retained HTML versus built default V2 HTML, and unexecuted source audits. The existing Settings mutation/retry/event ordering, DOM-error observer, Player creation/credential consumers, strict Deno configurations, alias-specific permissions and single atomic reward authority are retained.

The original three evidence files were locally re-hashed to their published blob identities before edits. Documentation validation checks original-row/alias preservation, counts, source identity syntax, companion links, scoped method/path uniqueness and whitespace. Complete source checkout/archive and execution of the original source audits were unavailable; no excerpt, fixture, stale workflow or production probe is credited as a substitute.

The backlog is unchanged in this continuation, preserving all 50 task objects and all dependency links. The PR remains within its existing five-file documentation/data scope. No application, bundle, runtime manifest, workflow, test, database, migration, secret, deployment or cloud configuration was modified.

MAP-01: complete remaining retained-wrapper/event-consumer and selected browser root-to-resolver closure; bundle readability and declaration tables are no longer the blocker.

MAP-02: finish every current Admin V2 client/controller operation, Player leaf-parser method acceptance/rejection (including unadvertised routes), all BFF forwarding bodies, and full function/worker/import-root reconciliation against the tracked source. The complete api filename denominator and Player outer precedence are now recorded, not substituted for those missing traces.

MAP-03: execute required original source audits against complete original inputs, finish evidence review and applicable final-head checks, then normal merge. A full checkout/archive was unavailable; no fixture or historical CI is substituted. Unknown hosted consumers remain downstream retention gates, not extra production-test prerequisites.

**Stop at REF-002. REF-003 has not started.** Do not merge this draft or grant completion credit until the remaining source requirements and applicable checks pass. Unobservable external consumers remain downstream retention gates, not a new production-testing requirement.
