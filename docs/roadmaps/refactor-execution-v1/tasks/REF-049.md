# REF-049 — Retire one superseded source-patching hotfix workflow

Status: PLANNED. Risk: R3. Dependencies: REF-045, REF-046, REF-048. Maps to ARCH-700. Reconcile #730/#731/#735 before activation; do not create another release-control owner.

## Read first

`.github/workflows/production-admin-join-code-read-503-hotfix-v2.yml`, its exact dispatch/caller history, current release-controller workflow/contracts and web-session regression tests. The observed historical workflow contains source-string replacement for an old join-code proxy path; that alone does not prove it is unused.

## Allowed change

Only this workflow's approved retirement/archive record and exact now-obsolete references/tests. Preserve all active production/recovery/release requests and their source/artifact/DB/staging/approval checks. No broad workflow consolidation, schedule change, branch-protection edit, secret/deploy key or cloud action. Budget: eight meaningful files.

## Implementation

1. Determine whether the workflow remains a supported recovery path or is referenced by an active controller/authorization. Check current PR #735 changes and workflow dispatch history before deciding.
2. Map each guard/behavior it provides to the current canonical release path. Missing replacement evidence blocks retirement; repeated-looking YAML is not duplicate functionality proof.
3. After owner approval and zero required consumers, archive the historical procedure/source reference outside executable workflow discovery and remove only its obsolete active registration.
4. Update exact regression references while preserving equivalent guard coverage. Do not reduce required economic/auth/recovery assertions, disable failing workflows or remove an active source-convergence check.
5. Validate remaining workflow YAML, embedded scripts, release-input discovery and manual recovery instructions without dispatching a production workflow.

## Acceptance and validation

One genuinely superseded workflow is retired with documented replacement/rollback and no lost active control. Run root `test:web-session-release`, `test:release-platform`, relevant recovery/deployment-readiness contracts, YAML/script parsing, secret/diff and architecture checks. Release state remains unchanged.

## Stop / rollback

Active recovery usage or unresolved release ownership blocks removal. Revert the workflow archive/reference change only after owner review; never dispatch it, overwrite live source or mutate production to test rollback.
