# REF-007 — Replace Contract progress GET forwarding with a local adapter

Status: VERIFIED_COMPLETE. Risk: R2. Dependencies: REF-005. Maps to ARCH-400/ARCH-700. Recheck #668 context ownership.

## Read first

`backend/supabase/functions/admin-api/gameRoutes.ts::handleGameRead` and its `contractProgressMatch`; `common.ts::proxyClassroom`; `backend/src/domains/contracts/api/staffContractHttpHandler.ts`; existing Contract progress read/repository tests; REF-004 fixture.

## Allowed change

Only the GET `/games/:game/contracts/:contract/progress` adapter, a proposed `admin-api/contractProgressReadOperation.ts`, the existing Contracts-owned read seam if extraction is necessary, and tests. No review/reward mutation, Store/Settings migration or blanket proxy deletion. Budget: ten meaningful files.

## Implementation

1. Trace the downstream `/staff/game-sessions/:game/contracts/:contract/progress` behavior: auth, ownership, identifiers, filters, ordering, pagination and response envelope.
2. Reuse its current Contracts read use case/repository directly under the Admin guard. If it exists only in an HTTP handler, extract the smallest read operation without changing SQL or its scope predicates.
3. Adapt the approved Admin context explicitly; do not cast an untrusted browser identity into Staff authority. Preserve any security/throttling checks previously enforced downstream.
4. Replace just the selected `proxyClassroom` call with local invocation and current JSON/header mapping. Keep other aliases and proxy paths unchanged.
5. Record the removed network hop and compare both implementations using the same synthetic fixtures. Assert no outbound Classroom fetch for this route and no increase in database calls.

## Acceptance and validation

Valid owner, missing contract, wrong game, unauthorized Staff, malformed identifier, empty progress and repository failure retain their established response contract. Reads remain read-only. Run backend `test:admin-api`, `test:player-contract-acceptance`, `test:player-contract-lifecycle`, relevant Admin Contracts tests and shared typecheck/auth/architecture gates.

## Stop / rollback

Stop if downstream permission/rate-limit parity is unresolved or a schema change is proposed. Revert the local adapter and its wiring as one source change; no data correction is involved. Do not claim Classroom retirement from one removed call.

## Execution and closeout

This is repository-only completion of REF-007a, REF-007c and the separately scoped REF-007b closeout. The completion status is effective only after this closeout passes its applicable checks and is normally merged into main; an unmerged copy is a proposed update. It is not production certification or permission to start Task 8.

Implementation PR #748 was normally merged as `62e1730956739743c9e14d0e03f6c65b03503f11` from accepted head `ad4e19d68c10511214d3b1d54db879f4b0e8d3d7`, based on `a3ec93f60817746e34e124b59210f3e8ab5ebf32`. The actual merge tree `f4cfa0d172d677c5e5a8e5d100ecff00c6c51b78` is identical to the verified candidate. Complete command, artifact, source and scope records are in [the existing evidence](../../../operations/evidence/refactor-execution-v1/REF-007/contract-progress.json). The closeout PR discussion records its own checked head and actual merge, rather than inventing a prospective self-referential SHA.

Only the selected Admin Contract-progress GET calls the existing Contracts reader locally. Its Classroom HTTP hop falls from one to zero; the reader implementation remains one, the ordered domain reads remain two, and the fresh owned-game lookup and additional Staff limiter remain one each. Both Admin and Staff limiters, server-derived context, response DTO/error/header mapping and the existing Admin query omission are preserved. The other review/reward/access-reset routes remain unchanged, and no economic write is introduced.

Pinned CI passed Admin API **263/263**, including **20 REF-007 parity cases**, Contract acceptance **45/45**, Contract lifecycle **1/1**, Player security **59/59**, full backend smoke, TypeScript and all **28 Edge roots**. The complete diagnostic archives were downloaded and verified against their GitHub SHA-256 digests. These are CI executions, not local Deno runs. The exact-head workflow census and post-merge security results are recorded in the evidence.

The complete deterministic inventory was reconciled without changing its generator or inherited ceilings. A comment-only lexical clarification is not functional debt removal. The PR-bound verification manifest preserves all required checks. Explicit child REF-007c transfers one retired GET proxy-call registration to the exact in-process limiter-metadata literal, removing the old call allowance while retaining 37 registered identities and 40 occurrence allowances. All other 36 sites, all 23 removal targets, the checker and original REF-005 history remain intact. The two implementation children have eleven distinct paths, including seven source/test paths; no source scope was silently enlarged.

The original compiler, fixture, inventory and exact-site qualification failures remain recorded. The small boolean-discriminant corrections preserve declared true/false results under the existing compiler settings; no formulas, branch bodies, RPCs or reward authority were replaced. The existing REF-004 progress fixture now exercises the actual reader and its HTTP 200 contract rather than the removed transport's arbitrary 202 stub. Its other denial, review/reward, reset and replay cases remain.

The standalone Beta Security workflow is path-filtered out for this PR; its configured actual-main run `35920687490` passed after the implementation merge and before closeout. No skipped workflow or synthetic/disposable browser run is presented as hosted staging or production evidence.

REF-007b changes only this task document, the existing REF-007 evidence and REF-007's four status/identity/evidence fields in `backlog.json`. All other 49 task objects and every dependency are preserved. PR #668's context/Dashboard/global ledgers and #736's authentication correction remain separately owned. No deployment, live SQL, secrets or cloud settings were changed. Remaining Classroom callers and source-retirement gates still apply.

Next exact task: **REF-008, PLANNED and unstarted. Stop after REF-007.**
