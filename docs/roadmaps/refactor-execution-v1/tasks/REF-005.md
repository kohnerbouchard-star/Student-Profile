# REF-005 — Extend existing architecture ratchets for the selected seams

Status: VERIFIED_COMPLETE. Risk: R1. Dependencies: REF-003, REF-004. Maps to ARCH-001 extension; its original completion remains intact.

## Read first

`scripts/architecture/architecture-ratchet-v2.mjs`, the inventory generator, `scripts/high-priority-boundary-ratchet.mjs`, `scripts/legacy-runtime/legacy-browser-transport-ratchet.mjs`, root audit scripts and REF-003 dispositions.

## Allowed change

Existing architecture tooling and its focused fixtures, plus one reviewed scoped baseline derived from current source. No application changes, new test framework, broad workflow rewrite or raised debt ceilings. Budget: eight meaningful files.

## Implementation

1. Reuse the current scanner/ratchet conventions; identify which desired rules already exist and add only missing assertions.
2. Freeze the exact allowed Classroom forwarding/import families and selected browser interception sites. Reject new unregistered sites while permitting measured existing debt to decrease.
3. Add a deletion-proof check for records selected by this program: canonical replacement, caller evidence, disposition and ownership must exist before the path is removed. Do not let an arbitrary `approved=true` replace evidence.
4. Preserve zero-tolerance checks for browser database access, direct money/inventory authority bypass and unscoped state. Keep production/test/fixture classifications distinct so useful fallback code is not banned.
5. Add negative fixtures for a new proxy caller, new fetch monkey patch, direct economic mutation and missing removal record; add positive fixtures for an existing permitted seam and ordinary image/error defaults.

## Acceptance and validation

A clean exact-main scan passes without relaxing pre-existing ceilings. Every injected forbidden pattern fails the relevant check. Repeated generation is deterministic. Run `npm run audit:architecture`, `npm run audit:high-priority-boundaries`, `npm run audit:legacy-runtime`, focused tool tests and secret/diff checks. Document exclusions precisely; do not exclude an entire domain to silence a match.

## Stop / rollback

If a rule cannot distinguish legitimate data/read behavior, narrow the detector and retain a review candidate instead of blocking or deleting working code. Revert only this incremental tooling change; do not remove inherited guards.

## Bounded execution — 2026-09-23

Owner request: Task 5 only. Starting main: `576bc693416a4eda4bb9e8809fad3258f2b0611f`; both REF-003 and REF-004 are VERIFIED_COMPLETE. No active REF-005 owner branch or PR was found. The existing global ledger/context (#668), Player auth (#736), release controls (#730/#731/#735), Player CSS (#624), dependency (#620) and Living World (#690) owners remain separate.

The [scoped baseline](../../../operations/evidence/refactor-execution-v1/REF-005/seam-baseline.json) doubles as this task's exact seven-file scope record and evidence ledger. The eighth file is reserved only for an existing policy's PR-bound verification manifest if required by actual CI. No package or workflow registration is changed: the current architecture audit invokes the additive helper and the existing legacy-runtime test entrypoint registers its focused fixtures.

Inherited aggregate debt caps, direct-browser database denial, money/Inventory owner checks, simulation-scope guard, retired-marker checks, high-priority security controls and legacy-runtime retirement policy remain unchanged. New site identities prevent an added caller or interception site from hiding under unused aggregate budget. The scanner permits measured debt reduction; a new site requires explicit review rather than an automatic baseline rewrite.

Deletion evidence is fail-closed for the selected REF-003 records. It must identify the old source, disposition and owner, an existing replacement, all caller surfaces, an owner retirement decision and parity test/results through actual matching Git blob references. Ordinary defaults are not deletion candidates. No removal is approved by REF-005. The structural checker cannot certify the truth of human approval, external absence, SQL atomicity or runtime evidence; existing retirement windows and source-retention gates remain binding.

Initial local verification: 29 synthetic focused tests passed on Node 22.16.0. No complete local checkout or local Deno execution was available. Pinned GitHub full-checkout CI must qualify the initial exact-source census and run the inherited audits and registered tests. Empty initial site/target allowlists intentionally fail closed until the emitted census is reviewed; this first qualification run is not acceptance. Exclusions and lexical/dataflow limitations are explicit in the baseline.

Status remains IN_PROGRESS until exact-head checks, reviewed normal merge and actual-merge closeout. Preserve every other task object and dependency. No application, UI, migration, database, secret, cloud, dependency or deployment change. Stop after REF-005; REF-006 is unstarted.

## Actual implementation merge and closeout — 2026-09-23

**VERIFIED_COMPLETE** for the bounded REF-005 architecture-tooling extension. PR **#744** was normally merged at **2026-09-23T05:50:26Z**. Final implementation head: `efd5142e1fa744bc7b2126d10da0a834cc6b7a6a`. Actual implementation merge: `1fb96a613f5b04548b5ccb784170f72d5b505ae1`. Both share tree `82c4717fee0aece1a8157d891e4ee0c571f344d7`. The original ARCH-001 acceptance and earlier task records remain unchanged. This is not a production or whole-application certificate.

The reviewed baseline registers **37 lexical identities / 40 occurrences across 20 source files**, including **9 browser interception identities**, and protects **23 selected REF-003 paths**. No application debt was removed. The original inventory and all numeric ceilings remain unchanged. The actual implementation is seven changed paths, with 331 new tooling/test source lines and seven additive entrypoint/registration lines; no new package, workflow, dependency or runtime framework.

All **eight applicable final-head workflows and Vercel passed** before merge. Run identities are in the baseline publication object. Legacy Runtime Retirement Audit `35823169636`, job `107059179596`, passed **63 tests total**, including **38 focused REF-005 cases plus one deterministic repository census**, retained REF-003 checks and the original retirement tests. Repository Quality `35823169617` passed root audits/source checks, credential scanning and dependency controls. Business Player Store Cutover V2 `35823169639` passed source/architecture verification, backend TypeScript and Edge roots, disposable database replay/concurrency/two-game tests, connected Buyer/seller journeys and the full Chromium suite. Disposable CI activity is not a live production action.

Initial empty-allowlist qualification at `875b16a7475b11726b5a97d727a5503e293e90d5` failed intentionally and remains recorded with its artifact identity. The reviewed intermediate head `1348b1b176c7450b8bf6ceb5391237266f55754f` passed Repository Quality `35822757688` and Legacy Runtime Retirement Audit `35822757712`; those results are not assigned to the later final head. Final review additionally protected legitimate JSON/Markdown replacements, rejected directory placeholders, and required the parity-result record itself to confirm PASS, exit code zero and exact source/command/test blob.

Local Node 22.16.0 validation passed 38 focused tests and the exact seven-path `git diff --check` in an isolated Git fixture. Original modified-file blobs were reproduced before additive changes, and the resulting diff matched the remote seven-file comparison. The other 49 task objects and every dependency were unchanged. A complete local application checkout and local Deno execution were unavailable; full-checkout GitHub CI supplied repository execution.

Only the task, its backlog fields and this task's existing baseline publication metadata receive closeout updates through a separate normal PR. Existing site/target/source classifications, initial failed qualification, all exclusions and proof limits are preserved. No prospective closeout merge SHA is embedded. Structural evidence consistency does not prove an approver's identity, live caller absence or test authenticity; the existing retirement policy remains binding. **No deletion is approved here.**

No application/UI implementation, generated bundle, migration, inherited baseline, inventory generator/output, compiler setting, package/lockfile, workflow, secret or live cloud/database configuration changed. No production release/deployment was dispatched. **REF-006 remains PLANNED and unstarted. Stop after REF-005.**
