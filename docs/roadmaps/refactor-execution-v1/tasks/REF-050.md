# REF-050 — Re-measure every module and certify only the proven scope

Status: PLANNED. Risk: R0. Dependencies: all REF-001 through REF-049 and every registered required child. Maps to ARCH-000 measurement and final scoped acceptance, not an invented new feature phase.

## Read first

Current main, all task evidence/PR merges, canonical beta/controller ledger, current architecture/legacy inventories, runtime release evidence and [scoring rules](../SCORING.md). Resolve which dependencies are actually complete versus source-only, verification-only or blocked.

## Allowed change

Final module scorecard/metrics, remaining-debt register, exact-source evidence summary and controller-owned status reconciliation. No corrective source changes hidden in certification. Proposed evidence files: `module-scorecard.json`, `module-scorecard.md`, `remaining-debt.json`, `acceptance.md`. Budget: eight documentation/data files.

## Implementation

1. Recompute the same classified inventory on exact merged main. Report production source/test/fixture/generated/archive/migration denominators separately; compare like-for-like classifications with REF-003.
2. For every module in the initial task coverage map report confirmed-dead removals, retained required compatibility, unresolved candidates, boundary violations, runtime coverage and evidence confidence. A moved file is not deleted functionality.
3. Score architecture, maintainability, verification and approved-requirements fit only using the explicit rubric. Mark uninspected criteria NR rather than inventing a numeric score. Do not equate test-file count with executed coverage or fixture UX with human enjoyment.
4. Verify each accepted task at its merge SHA and final-main relevant gates, including no reopened proxy/deep-import/ledger/privacy violations. Preserve failed attempts and unresolved evidence gaps.
5. Separate repository-scoped acceptance, staging verification and production certification. If REF-048 or any required runtime gate remains blocked, issue a partial progress report with BLOCKED completion, not a full refactor/production claim.

## Acceptance and validation

Every score/count links to reproducible source or observed evidence; all remaining work is explicit. Run document/JSON/link/dependency checks and applicable exact-main application/architecture suites per VALIDATION.md. Live evidence must identify its own exact source/artifact/configuration, with authorized authenticated/economic probes where required.

## Stop / rollback

No automatic 9/10 target, zero-legacy claim or assumed all-app completion. A regression opens its own bounded corrective owner. Revert inaccurate documentation if necessary, not source/database history. Publication and measurement never authorize release.
