# REF-008 — Converge the three Admin Contract review aliases

Status: IN_PROGRESS. Risk: R3. Dependencies: REF-007. Maps to ARCH-208/ARCH-400. Resolve #668 ownership before changing context propagation.

## Read first

`admin-api/gameRoutes.ts::handleGameWrite`: `submissionDecisionMatch`, `submissionReviewMatch`, `progressReviewMatch`; the Contracts Staff review handler/use case, review transaction, `player_contract_progress` lookup and REF-004 parity cases.

## Allowed change

These three review translations and one Contracts-owned review application adapter plus tests. Proposed Admin translator: `admin-api/contractReviewOperation.ts`. No reward-issuance endpoint change, status-model expansion, new RPC or general router rewrite. Budget: twelve meaningful files.

## Implementation

1. Freeze method/path behavior: decision and submissions-review currently accept POST/PATCH and forward as POST; progress-review accepts POST. Preserve this distinction.
2. Move the submission-only lookup behind a Contracts read repository with both game and submission predicates. Retain the established `contract_submission_not_found` response and reject cross-game/contract mismatches before the mutation.
3. Normalize each alias into one internal command carrying approved Staff/game context, contract/progress identity, original body and current request/idempotency identity. Do not silently change payload hashing.
4. Invoke the existing review authority once. Carry all validations/audit/replay checks from the downstream path into the local use case; do not implement another status transition table.
5. Return the original alias envelope/status and keep unrelated `handleGameWrite` branches unchanged. Record zero Classroom fetches for these three aliases only.

## Acceptance and validation

All aliases yield the same authoritative transition for equivalent valid inputs. Test wrong game/contract, unsupported method, missing progress, repeated review, conflicting replay, repository failure and immutable audit behavior. Assert one review transaction and no extra reward issuance. Run backend `test:admin-api`, `test:admin-local-mutations`, Contract acceptance/lifecycle, economic-ledger invariants and focused disposable-database replay/race acceptance.

## Stop / rollback

Stop if review and reward atomicity cannot be preserved by reusing the existing command. Do not split SQL effects into independent service calls. Revert this adapter PR, preserving all independently merged auth fixes and database history.

## Current execution

Owner requested Task 8 on verified main `e8e9f6fd47b6faaebee44c075a7837b10481b1e5`. Existing owner branch is `refactor/ref-008-local-contract-review`, draft PR #750. The exact twelve-path scope and acceptance boundaries are recorded in `docs/operations/evidence/refactor-execution-v1/REF-008/contract-review.json`. The PR-specific verification manifest is `docs/operations/contracts/player-cross-cutting/pr-750.json`.

The first source slice prepares one shared Admin review command and a Contracts-owned, read-only submission identity repository. The command preserves the original request, normalization, POST/PATCH distinctions, transport identity precedence and decision-only approval reward intent. Its context checks precede lookup; the repository retains both game and submission predicates. Preparation tests exercise these components without invoking a review or reward mutation.

This slice does not yet replace the three router branches, invoke the local review authority, establish downstream limiter/ownership parity or certify database replay/race/audit behavior. Existing review routes still forward. Passing preparation tests alone cannot complete REF-008 or prove the target zero-network-hop behavior. Router activation, authoritative mutation parity, deterministic inventory, full exact-candidate verification and checked closeout remain required on this same branch.

Important inherited behavior: the existing REF-004 domain test explicitly records same-key approve then reject as two accepted review writes. This refactor must preserve that behavior rather than inventing an idempotency/conflict-rejection guarantee. Only successful decision approval automatically requests rewards; review and its subsequent reward issuance are separate existing actions, including their partial-failure boundary. The existing reward transaction remains unchanged.

PR #668's context/bootstrap/Dashboard/global-ledger ownership is preserved; its actual overlapping-file patch was inspected and the bounded non-overlap decision is recorded at PR #668 comment `5806491552`. No source from #668 or #736 is imported or merged. Main, database history, cloud configuration, UI, authentication/client factories and release controls remain unchanged.

Stop after REF-008. REF-009 is not started.
