# REF-006 — Move the retained Messaging dispatcher to its domain owner

Status: VERIFIED_COMPLETE. Risk: R2. Dependencies: REF-005. Maps to ARCH-400/ARCH-402. Recheck #668/#736 before touching Player composition roots.

## Read first

`backend/supabase/functions/player-api/runtime.ts`, Classroom index and the exact Messaging dispatcher import resolved by REF-002; `backend/src/domains/messaging/api/`; the existing Player Messaging/rate-limit tests. Search current source for `classroom-api` and Messaging dispatch references rather than assuming the historical import remains.

## Allowed change

One dispatcher implementation, its two composition-root imports, a temporary re-export only if required, and focused tests. Proposed destination if no suitable current owner exists: `backend/src/domains/messaging/api/playerMessagingDispatch.ts`. No auth/client factory, route table expansion, UI or database edits. Budget: eight meaningful files.

## Implementation

1. If canonical Player code already imports a neutral/domain-owned dispatcher, record verification-only disposition and remaining references; do not move it again.
2. Otherwise move the existing dispatcher byte-for-byte first, adjusting only imports. Domain-specific route parsing belongs under Messaging; unrelated composition remains in the Player root.
3. Wire Player directly to the new owner. Classroom may import the same implementation or temporarily re-export it one-way; the new owner must never import Classroom.
4. Preserve supported methods, rate-limit keys, context forwarding, participant/game checks, idempotency and safe response mapping. Do not re-create session/service-role dependencies during dispatch.
5. Characterize both roots and check the runtime import closure/cycles. Remove only genuinely unused imports; retain still-required compatibility exports until REF-048.

## Acceptance and validation

Player-to-Classroom implementation imports for this dispatcher reach zero; exactly one implementation remains. Same requests produce the same status/body and service-call counts through both roots. Run backend `test:player-messaging`, `test:player-security`, `test:player-request-scope`, `typecheck:all`, Player `messaging-connected`, and shared architecture/auth checks.

## Stop / rollback

Stop if the owner PR is active or movement would redesign cross-domain routing. Roll back by reverting this source move/import wiring without changing route behavior or resurrecting an independently retired runtime.

## Execution and closeout — 2026-09-23

This is repository-only completion of REF-006a and REF-006b, effective when this three-document closeout is checked and normally merged into main. An unmerged copy is a proposed status update. It is not production certification, completion of all ARCH-400/402 work, or permission to begin another task.

Implementation PR [#746](https://github.com/kohnerbouchard-star/Student-Profile/pull/746) was normally merged at `f823928b30cbdd740f5c304abe28e6a21b730ad3` from accepted head `dee1b16d77cc75a4ed32f6be51145fe1002d3617`, based on `7e2f1022c601c33c2429de10b36a86dfcb13d2f8`. Both the tested PR merge ref and the actual merge have the accepted tree `2a6437dddbc78deec220567d389836af42a79bbf`. Full command, artifact, ownership and measurement evidence is in [the existing scope record](../../../operations/evidence/refactor-execution-v1/REF-006/messaging-dispatch.json) and the [qualification receipt](https://github.com/kohnerbouchard-star/Student-Profile/pull/746#issuecomment-5791920786).

The single dispatcher moved into `backend/src/domains/messaging/api/playerMessagingDispatch.ts`. Its declaration and dispatch body are unchanged apart from seven relative import occurrences resolving to the same targets. Player changes one import specifier; Classroom retains an explicit one-way value/type export. No new cycle or Classroom dependency enters the domain implementation. This does not certify a whole-repository zero-cycle graph. Source imports to the Classroom implementation fall from one to zero; implementations remain one; the retained export increases from zero to one; no source path is deleted. Methods, route ordering, authentication/client injection, game/participant checks, limiter behavior, idempotency and safe mapping remain intact.

Pinned CI ran the complete named commands: backend Messaging **32/32**, Player request scope **71/71**, Player security **59/59**, including **17** Messaging dispatcher cases; full backend smoke exited zero; `typecheck:all` passed TypeScript and all **28 Edge roots**. The complete downloaded logs and their checked artifact digests resolved the earlier evidence gap without rerunning or weakening tests. Local Deno remained unavailable; these are CI results, not claimed local executions.

The accepted candidate has **38** accounted-for workflow runs: **35 successful**, three expected historical/manual release skips, no failure or pending run. Player standalone verification including `messaging-connected`, Chromium, all four critical Store/FX jobs, Repository Quality, Beta Security, Supply Chain, Admin browser and Multiplayer/load checks passed. The separate Admin API workflow was path-filtered out; its underlying typecheck/tests ran in the successful shared commands. Messaging's hosted connected job was not run; its harness success is not live staging evidence.

The eight-path source tranche preserves the initial failed inventory qualification and updates only the reviewed source bindings and two generated source counts. The separately scoped REF-006b changes only this task file, the existing scope/evidence JSON and REF-006's four status/identity/evidence fields in `backlog.json`; all other 49 task objects and every dependency are preserved. Its PR discussion records its checked head and actual closeout merge receipt rather than a prospective self-referential SHA.

No blocker remains for this bounded source task after the closeout merge. The retained Classroom export still requires REF-048 and external-retention evidence before removal. PR #736's authentication correction and #668's context/global ledgers remain separate and untouched. No live release, SQL, secrets or cloud settings were changed. Next exact task: **REF-007, PLANNED and unstarted; stop after REF-006.**
