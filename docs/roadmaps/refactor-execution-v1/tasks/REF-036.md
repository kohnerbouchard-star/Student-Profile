# REF-036 — Bound Progression reward requests to canonical Economy authority

Status: PLANNED. Risk: R3. Dependencies: REF-022. Maps to ARCH-208.

## Read first

`backend/src/domains/progression/api/playerProgressionHttpHandler.ts`, Progression services/contracts/tests, current milestone/reward execution path and Economy-owned ledger commands. Read existing progression simulation and abuse-threshold tests.

## Allowed change

One currently implemented progression reward request/receipt adapter and its exact owner imports/tests. No XP curve, milestone definitions, eligibility, reward amounts, timers, unlock UI, achievements or SQL changes. Budget: ten meaningful files.

## Implementation

1. Identify the existing trigger, eligibility calculation, grant transaction, deduplication/replay identity and effect receipt. Distinguish a read-only projection from actual reward issuance.
2. Replace the selected deep dependency or inline database translation with a narrow Progression-to-authoritative-command contract; reuse the existing Economy public seam.
3. Keep the current atomic grant/ledger/progression transaction intact. Do not emit a second payment after marking a milestone complete or reward both the event and its replay.
4. Preserve anti-abuse thresholds, game/player scope and display/notification behavior. Do not classify unclaimed rewards or empty progress as obsolete code.
5. Test duplicate/out-of-order events, already-granted rewards, insufficient eligibility, wrong game, dependency failure and concurrent claims with existing simulation fixtures.

## Acceptance and validation

One authoritative grant path and unchanged eligibility/value/effect counts. Replayed events never issue a second economic reward. Run backend `test:player-progression`, `test:economic-ledger-invariants`, root `test:progression-simulation`, `test:admin-progression`, relevant notification cases and shared checks.

## Stop / rollback

If no illegal/unclear reward dependency remains, document verification-only disposition. Do not create a new service merely to add files. Any balancing change or SQL correction requires separate scope. Revert the selected adapter, never player progression or ledger data.
