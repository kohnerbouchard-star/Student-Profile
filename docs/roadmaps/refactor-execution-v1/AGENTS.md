# Agent instructions for the refactor package

Follow repository-root AGENTS.md first. This directory is a decomposition companion, not a new source of global completion or deployment authority.

Read README.md, SOURCE-RECONCILIATION.md, EXECUTION-CONTRACT.md, VALIDATION.md, SCOPE-INTAKE.md, backlog.json and exactly the selected task. Recheck current main and active owners; never execute a stale ticket literally when source or ownership has changed.

Preserve 50 stable parent IDs. Register explicit children for a genuinely oversized task; do not silently broaden its scope. All package publication states remain PLANNED until real implementation/evidence changes them. Missing evidence is a blocker, not a passing result.

Do not change global beta/context/release ownership through this folder. Never rewrite historical migrations, apply production SQL, weaken auth/ledger/isolation tests, change gameplay or remove defensive fallbacks based on lexical markers. Code cleanup is not a UI redesign or a framework migration.
