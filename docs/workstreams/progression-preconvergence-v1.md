# Progression convergence and acceptance ledger v1

Status: `CONVERGED_EXACT_HEAD_VERIFICATION_IN_PROGRESS`

Authority: PR #261, branch `agent/progression-reputation-achievements-v1`.

Final predecessor: Messaging PR #248, merge SHA `955c97a9a2c8e734cfd89e5202a052afc74edacd`.

Production and canonical staging remain unchanged. PR #261 remains draft and unmerged.

## Convergence checkpoint

The one authorized synchronization with post-Messaging `main` is complete. The branch is mergeable, zero commits behind `main`, and preserves a true Messaging merge parent.

Convergence checkpoint before this evidence commit: `56c6ae345e2785d84f756fabe04f08e25689645d`.

The shared-file reconstruction preserved current World, Business, Crafting, Marketplace, Messaging, Story, Notifications, Store, Inventory, Contracts, security, release and Admin behavior. Progression contributions were applied additively rather than selecting obsolete pre-Messaging shared files.

Temporary convergence workflows, scripts and generated bytecode were removed. The permanent Progression verification workflow is restored.

## Final migration authority

The final four-slot Progression migration family is:

1. `20260721160000_add_progression_reputation_runtime_v1.sql`
2. `20260721161000_fix_progression_read_volatility_v1.sql`
3. `20260721162000_harden_progression_event_idempotency_v1.sql`
4. `20260721163000_rebalance_progression_curve_v1.sql`

No provisional Progression migration identities remain. No fifth Progression migration is planned.

## Permanent runtime scope

Progression owns permanent implementation and focused verification for:

- experience and a bounded 20-level curve;
- skills, prerequisites, costs and atomic unlock;
- bounded achievement rewards and atomic reward claim;
- immutable achievement completion;
- country, career, story and relationship reputation;
- authenticated private Player state and filtered public profiles;
- owner-scoped Admin review, correction and immutable correction history;
- wrong-game, wrong-player, expired-session, paused-game and ended-game behavior;
- fail-closed rate-limit and schema-unavailable behavior.

Player self-read responses use a strict allowlist and reject unexpected UUID-shaped or private audit fields. Public profiles expose completed achievements and only reputation rows explicitly marked public. Claim state, private progress, rewards and Admin audit data remain private.

## Publication surfaces

The deployable Classroom API now publishes:

- `GET /players/me/progression`;
- `POST /players/me/progression/skills/{skillId}/unlock`;
- `POST /players/me/progression/rewards/{rewardId}/claim`.

The deployable Admin API now publishes:

- `GET /games/{gameId}/progression`;
- `GET /games/{gameId}/progression/corrections`;
- `POST /games/{gameId}/progression/players/{playerId}/corrections`.

Player transport uses the post-Messaging composed adapter architecture through `progression-backend-routes.js`. The Capability Manifest remains descriptive and is not used as a dispatcher. Player and staff rate limits remain centralized and fail closed.

## Event compatibility and abuse resistance

Stable compatibility fixtures remain defined without copying predecessor internals for:

- `business.operation.completed`;
- `crafting.recipe.completed`;
- `market.order.settled`;
- `story.chapter.completed`.

Trusted event ingress validates game and Player UUIDs, binds source domains to event types, rejects malformed/stale/future events before RPC, serializes per-Player processing, provides stable source-event uniqueness, replays duplicate delivery and committed success, rejects mutated source details and conflicting replay keys, and denies new awards for inactive games.

Focused coverage includes duplicate, delayed and out-of-order delivery, source/event mismatch, wrong-game events, malformed payloads, stale events, conflicting replay keys and committed-success retries.

## Deterministic economic thresholds

The permanent simulations fail closed when any bound is exceeded, including:

- level 10 earlier than day 10 or level 20 earlier than day 30;
- achievement skill rewards above 16;
- specialization share above 36% or HHI above 0.30;
- Business/Crafting/Marketplace aggregate spread above 0.12;
- positive relationship reputation above 30 points per day;
- coordinated loops above 2,640 XP per day;
- catch-up multiplier above 1.15 or catch-up ratio outside 0.70–1.10;
- more than one award from duplicate stable-source delivery;
- any new award in paused or ended games;
- failure to replay a committed event after pause or end.

Current deterministic results include level 10 on day 18, level 20 on day 71, HHI 0.28, dominant-path spread 0.08, one applied plus 999 replayed farming attempts, day-one reputation gain 30, coordinated-loop ceiling 2,640 XP/day and late-player ratio 1.061.

## Exact-head defect corrections

The first post-convergence matrix exposed and corrected two integration-only defects:

- Deno did not narrow the pagination result union through `!pagination.ok`; both call sites now use `pagination.ok === false`;
- the post-Messaging Player scope verifier lacked permanent Progression paths; it now permits only the Progression workflow, Admin surfaces, domain files, rate-limit test, final migration family, workstream ledger and deterministic scripts.

The bounded carrier used for these corrections was closed without merge and contains no feature authority.

## Acceptance checklist

- [x] exact Messaging merge SHA recorded;
- [x] final Progression migration range assigned and rekeyed once;
- [x] zero provisional migration references remain;
- [x] one synchronization with final predecessor `main` completed;
- [x] shared files reconstructed additively from final `main`;
- [x] migration uniqueness and monotonic ordering verified;
- [x] Classroom and Admin API publication complete;
- [x] Player adapter, normalizer, empty model and page publication complete;
- [x] Admin loader and review publication complete;
- [x] centralized Player and staff rate limits complete;
- [x] experience, levels, skills, rewards, achievements and all four reputation types covered;
- [x] public/private profile boundaries covered;
- [x] atomic claim/unlock and committed-success retry covered;
- [x] duplicate, delayed and out-of-order delivery covered;
- [x] malformed, stale, source-mismatched and wrong-game events denied;
- [x] paused, ended and session-expired behavior covered;
- [x] Admin authorization and immutable audit/history covered;
- [x] deterministic progression, inflation, concentration, dominance, farming, coordinated-loop, specialization and catch-up simulations green;
- [ ] replay from zero twice and database lint green on the final authenticated head;
- [ ] complete exact-head Backend, Player, Admin, browser, security, privacy, accessibility and repository matrix green;
- [ ] isolated-staging Progression acceptance green with zero residue;
- [ ] zero unresolved review threads confirmed on the final head;
- [ ] immutable head returned to the controller;
- [ ] explicit controller authorization obtained before leaving draft;
- [x] production unchanged.

After the authorized Progression merge, rerun the merged-main migration, route, capability, rate-limit, Classroom API, Admin API, Player publication, lifecycle, security, replay and repository convergence audit before PR #295 immutable release assembly.
