# Seed Roadmap Identity Amendment v1

Status: authoritative correction pending integration into `16-seed-data-roadmap-and-tracker-v1.md`  
Owner: PR #163 / `agent/seed-content-foundation-v1`  
Production authorization: false

## Correction to the current checkpoint

The earlier lexical editorial screen did not compare active instrument symbols and canonical identity fields. The later deterministic cross-layer audit supersedes any statement that the generated universe and curated active markets had zero identity conflicts.

Verified cross-layer result before canonicalization:

- 3,200 universe records checked;
- 96 curated active records checked;
- 92 reused-symbol identity conflicts;
- four missing active commodity-reference symbols;
- 69 exchange mismatches;
- zero active-ID collisions;
- 96 total blockers.

## Approved correction

- The ten country-qualified public-institution role families are approved for design use.
- The fifteen generated corporate roots remain placeholder-only and are not approved for production or public activation.
- Canonical exchange codes are `FGX`, `SBX`, `DHM`, and `AUX` for the first four active markets.
- Commodity references use class code `C`.
- Curated active identities are canonical wherever their symbols overlap the generated universe.
- The generator must overlay curated IDs, names, issuers, exchange identity, and compatible classifications while retaining exactly 3,200 unique, fail-closed records.

## Status rule

Milestone 1 and Milestone 2 identity gates remain incomplete until the reconciliation report shows:

- 96 exact canonical matches;
- zero conflicting symbols;
- zero missing symbols;
- zero exchange mismatches;
- zero active-ID conflicts;
- passing focused seed preflight and deterministic drift checks.

This amendment must be folded into the controlling roadmap before PR #163 is marked ready for review.
