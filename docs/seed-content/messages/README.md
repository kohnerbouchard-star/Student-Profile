# Seeded Messages

Machine-readable seeded messages are reusable content definitions only. They must not be treated as delivered notifications until an authoritative, game-scoped, idempotent delivery capability is merged and staged.
