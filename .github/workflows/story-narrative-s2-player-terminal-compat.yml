name: Story Narrative S2 Player Terminal Compatibility

on:
  push:
    branches:
      - feat/story-narrative-convergence-v1
    paths:
      - ".github/workflows/story-narrative-s2-player-terminal-compat.yml"

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out exact story branch source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: true
      - name: Run audited Player terminal compatibility repair
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/story-narrative/s2/repair-player-terminal-compat.py
          python3 -m py_compile scripts/story-narrative/s2/diagnostic-primary.py
          git diff --check
          rm -f scripts/story-narrative/s2/player-terminal-patch-section.txt
          rm -f scripts/story-narrative/s2/repair-player-terminal-compat.py
          rm -f .github/workflows/story-narrative-s2-player-terminal-compat.yml
          git add -A -- scripts/story-narrative/s2/diagnostic-primary.py scripts/story-narrative/s2/player-terminal-patch-section.txt scripts/story-narrative/s2/repair-player-terminal-compat.py docs/story/STORY-NARRATIVE-CONVERGENCE-ROADMAP.md .github/workflows/story-narrative-s2-player-terminal-compat.yml
          git diff --cached --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "fix(story): reconcile S2 Player terminal patcher"
          git push origin HEAD:feat/story-narrative-convergence-v1
