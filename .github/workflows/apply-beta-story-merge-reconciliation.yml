name: Apply Beta Story Merge Reconciliation

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write

concurrency:
  group: beta-story-merge-reconciliation
  cancel-in-progress: false

jobs:
  apply:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'docs/beta-controller-story-merge-v1'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          ref: docs/beta-controller-story-merge-v1
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Apply roadmap reconciliation and self-remove
        shell: bash
        run: |
          set -euo pipefail
          git pull --ff-only origin docs/beta-controller-story-merge-v1
          python scripts/apply-beta-story-merge-reconciliation.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/apply-beta-story-merge-reconciliation.yml
          git rm scripts/apply-beta-story-merge-reconciliation.py
          git add docs/roadmaps/econovaria-beta-completion-roadmap-v1.md
          git diff --cached --check
          git commit -m "docs(program): reconcile merged story delivery"
          git push origin HEAD:docs/beta-controller-story-merge-v1
