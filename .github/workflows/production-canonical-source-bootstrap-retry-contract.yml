name: Production Canonical Source Bootstrap Retry Contract

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/production-canonical-source-bootstrap-retry-v1.yml"
      - ".github/workflows/production-canonical-source-bootstrap-retry-contract.yml"
      - "scripts/production-canonical-source-bootstrap-runner.mjs"
      - "scripts/production-canonical-source-bootstrap-retry-contract.test.mjs"

permissions:
  contents: read

jobs:
  verify:
    name: Verify corrected production database binding remains fail-closed
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: "22.23.1"
      - run: node --check scripts/production-canonical-source-bootstrap-runner.mjs
      - run: node scripts/production-canonical-source-bootstrap-retry-contract.test.mjs
      - run: git diff --check
