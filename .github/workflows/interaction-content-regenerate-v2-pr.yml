name: Interaction Content Regenerate V2 PR

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
    paths:
      - ".github/workflows/interaction-content-regenerate-v2-pr.yml"
      - "package.json"
      - "scripts/apply-interaction-content-overrides.mjs"
      - "scripts/interaction-content-quality.test.mjs"

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: interaction-content-regenerate-v2-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  regenerate-interaction-content:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.head_ref == 'agent/seed-content-usable-v2'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out exact PR branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Wire interaction content into canonical seed build
        shell: bash
        run: |
          python - <<'PY'
          import json
          import pathlib
          path = pathlib.Path('package.json')
          package = json.loads(path.read_text(encoding='utf-8'))
          interaction_step = 'node scripts/apply-interaction-content-overrides.mjs'
          refresh_step = 'node scripts/refresh-seed-consumer-contract.mjs'
          build = package['scripts']['build:seed-beta-pack']
          if interaction_step not in build:
              if f' && {refresh_step}' in build:
                  build = build.replace(f' && {refresh_step}', f' && {interaction_step} && {refresh_step}')
              else:
                  build += ' && ' + interaction_step
          package['scripts']['build:seed-beta-pack'] = build
          test_file = 'scripts/interaction-content-quality.test.mjs'
          test_command = package['scripts']['test:seed-beta-pack']
          if test_file not in test_command:
              marker = 'scripts/campaign-content-quality.test.mjs'
              package['scripts']['test:seed-beta-pack'] = test_command.replace(marker, marker + ' ' + test_file)
          package['scripts']['validate:interaction-content'] = 'node --test scripts/interaction-content-quality.test.mjs'
          path.write_text(json.dumps(package, indent=2) + '\n', encoding='utf-8')
          PY

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: "22.23.1"
          cache: npm

      - name: Pin npm
        run: npm install --global npm@10.9.8

      - name: Install dependencies
        run: npm ci

      - name: Rebuild executable seed pack with authored interactions
        id: build
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run build:seed-beta-pack 2>&1 | tee /tmp/interaction-content-build.log

      - name: Publish sanitized interaction build failure
        if: steps.build.outcome == 'failure'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          {
            echo '### Interaction content V2 regeneration failure'
            echo
            echo '```text'
            tail -n 160 /tmp/interaction-content-build.log | sed -E 's#https://[^ ]+#<url-redacted>#g'
            echo '```'
          } > /tmp/interaction-content-build-comment.md
          gh pr comment "${{ github.event.pull_request.number }}" --body-file /tmp/interaction-content-build-comment.md

      - name: Stop after interaction build failure
        if: steps.build.outcome == 'failure'
        run: exit 1

      - name: Validate executable pack and authored interactions
        if: steps.build.outcome == 'success'
        run: |
          npm run validate:seed-beta-pack
          npm run validate:contract-content
          npm run validate:arrival-content
          npm run validate:campaign-content
          npm run validate:interaction-content
          npm run validate:physical-item-artwork
          npm run test:seed-beta-pack

      - name: Commit deterministic interaction content artifacts
        if: steps.build.outcome == 'success'
        env:
          ACCEPTED_SOURCE_SHA: ${{ github.event.pull_request.head.sha }}
        shell: bash
        run: |
          set -euo pipefail
          node scripts/refresh-seed-consumer-contract.mjs --source-sha "$ACCEPTED_SOURCE_SHA"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            package.json \
            docs/seed-content/executable/beta-pack-v1 \
            docs/operations/contracts/beta-seed-downstream-consumer-contract-v1.json
          if git diff --cached --quiet; then
            echo "Interaction content pack and consumer contract are already current."
            exit 0
          fi
          git commit -m "seed: regenerate authored interaction content pack"
          git push origin "HEAD:${{ github.head_ref }}"
