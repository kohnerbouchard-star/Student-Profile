# Edge Function Shared Code

Future home for code shared by Supabase Edge Functions only.

Domain business rules, persistence details, and cross-domain application logic should stay in `backend/src/`.
