# Notification Worker

Future Supabase Edge Function for processing notification jobs and delivery state.

This folder is a structure placeholder only. Do not add worker logic, request handlers, or schema changes in this checkpoint.
