# Economy Simulation Package

This package contains deterministic, fixture-driven balance and exploit
simulations.

It must remain independent from persistence, Seed catalogs, Edge Function
dispatch, Player/Admin route registries, and production configuration.
Simulation findings are advisory evidence for the owning domain or content
authority; they are never applied automatically.
