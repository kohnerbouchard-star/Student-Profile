# Backend Source

Backend source code will be organized by product domain and infrastructure boundary.

- `domains/` contains self-contained product modules.
- `platform/` contains infrastructure adapters and runtime integration points.
- `shared/` is intentionally small and reserved for narrow cross-domain helpers.
