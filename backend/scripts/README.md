# Backend Scripts

Scripts in this folder run outside backend runtime code.

- `local/` is for local development utilities.
- `import/` is for future legacy data import scripts.
- `maintenance/` is for future operational maintenance scripts.
