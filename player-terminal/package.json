{
  "name": "econovaria-player-terminal-standalone",
  "version": "7.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node tools/check-source.mjs",
    "smoke": "node tests/smoke.mjs",
    "test:api": "node tests/api-adapter.mjs && node tests/inventory-redemption-lifecycle.mjs",
    "audit": "node tools/audit.mjs",
    "verify": "npm run check && npm run smoke && npm run test:api && npm run audit",
    "dev": "python3 -m http.server ${PORT:-4173}"
  },
  "description": "Standalone Econovaria player frontend preserving the approved v7 design while adding a production-oriented, host-compatible player API adapter."
}
