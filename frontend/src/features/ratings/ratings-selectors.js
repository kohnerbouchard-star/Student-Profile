(function (global) {
  const app = global.EconovariaFrontend = global.EconovariaFrontend || {};
  app.modules = app.modules || {};
  app.modules.ratingsSelectors = {
    status: "inert",
    description: "Future display-only ratings selectors. No reward authority."
  };
})(window);
