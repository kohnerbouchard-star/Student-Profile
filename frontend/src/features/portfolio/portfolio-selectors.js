(function (global) {
  const app = global.EconovariaFrontend = global.EconovariaFrontend || {};
  app.modules = app.modules || {};
  app.modules.portfolioSelectors = {
    status: "inert",
    description: "Future display-only portfolio selectors. No state mutation."
  };
})(window);
