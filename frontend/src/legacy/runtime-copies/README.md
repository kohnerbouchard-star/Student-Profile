# Runtime Copies

This folder contains exact copies of active root runtime frontend files. These
copies are a clean baseline snapshot for the copy, test, transplant refactor.

Do not modify copied files in the same commit where they are copied. Extract
new modular behavior into `frontend/src/` files instead.
