(function (global) {
  const app = global.EconovariaFrontend = global.EconovariaFrontend || {};
  app.modules = app.modules || {};

  app.modules.main = {
    status: "inert",
    description: "Entry point placeholder for the frontend-only modular refactor."
  };
})(window);
