(function (global) {
  const app = global.EconovariaFrontend = global.EconovariaFrontend || {};
  app.modules = app.modules || {};

  app.modules.apiClient = {
    status: "inert",
    description: "Future API client. This scaffold does not change the active root API behavior."
  };
})(window);
