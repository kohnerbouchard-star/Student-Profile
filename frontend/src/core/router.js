(function (global) {
  const app = global.EconovariaFrontend = global.EconovariaFrontend || {};
  app.modules = app.modules || {};

  app.modules.router = {
    status: "inert",
    description: "Future view selection module for script-tag runtime compatibility."
  };
})(window);
