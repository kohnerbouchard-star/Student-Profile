(function (global) {
  const app = global.EconovariaFrontend = global.EconovariaFrontend || {};
  app.modules = app.modules || {};

  app.modules.table = {
    status: "inert",
    description: "Future table rendering helpers."
  };
})(window);
