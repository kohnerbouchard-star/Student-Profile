(function (global) {
  const app = global.EconovariaFrontend = global.EconovariaFrontend || {};
  app.modules = app.modules || {};

  app.modules.modal = {
    status: "inert",
    description: "Future shared modal helpers. No global listeners are installed here."
  };
})(window);
